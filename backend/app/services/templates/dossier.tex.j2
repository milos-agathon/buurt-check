<< preamble_content >>
\begin{document}

\section*{<% if language == "nl" %>Vastgoedrisico Dossier<% else %>Property Risk Dossier<% endif %>}
\textbf{<< address >>}

<% if building_year or building_use or floor_area %>
\begin{itemize}
  <% if building_year %>\item <% if language == "nl" %>Bouwjaar<% else %>Built<% endif %>: << building_year >><% endif %>
  <% if building_use %>\item <% if language == "nl" %>Gebruik<% else %>Use<% endif %>: << building_use >><% endif %>
  <% if floor_area %>\item <% if language == "nl" %>Oppervlak<% else %>Floor area<% endif %>: << floor_area >> m\textsuperscript{2}<% endif %>
\end{itemize}
<% endif %>

\textcolor{MutedText}{
<% if preparation_date %>
  <% if language == "nl" %>Voorbereid op << preparation_date >><% else %>Prepared on << preparation_date >><% endif %>
<% endif %>
}

\subsection*{<% if language == "nl" %>SAMENVATTING<% else %>EXECUTIVE SUMMARY<% endif %>}
<< executive_summary if executive_summary else ("Onvoldoende gegevens beschikbaar voor samenvatting." if language == "nl" else "Insufficient data available for summary.") >>

\section*{<% if language == "nl" %>Risicoscores<% else %>Risk Scores<% endif %>}
\begin{longtable}{p{0.30\linewidth}p{0.21\linewidth}p{0.24\linewidth}p{0.21\linewidth}}
\toprule
\textbf{<% if language == "nl" %>Categorie<% else %>Category<% endif %>} &
\textbf{<% if language == "nl" %>Score<% else %>Score<% endif %>} &
\textbf{<% if language == "nl" %>Ernst<% else %>Severity<% endif %>} &
\textbf{<% if language == "nl" %>Bron<% else %>Source<% endif %>} \\
\midrule
Noise &
<% if risks and risks.noise and risks.noise.score is not none %><< risks.noise.score >>/100<% else %><% if language == "nl" %>Niet beschikbaar<% else %>Unavailable<% endif %><% endif %> &
<% if risks and risks.noise and risks.noise.severity %>\textcolor{<< risks.noise.severity|sev_color >>}{<< risks.noise.severity|sev_label(language) >>}<% else %>\textcolor{MutedText}{<% if language == "nl" %>Niet beoordeeld<% else %>Not assessed<% endif %>}<% endif %> &
<% if risks and risks.noise and risks.noise.source %><< risks.noise.source >><% if risks.noise.source_date %> · << risks.noise.source_date >><% endif %><% else %><% if language == "nl" %>Geen bron<% else %>No source<% endif %><% endif %> \\

Air quality &
<% if risks and risks.air_quality and risks.air_quality.score is not none %><< risks.air_quality.score >>/100<% else %><% if language == "nl" %>Niet beschikbaar<% else %>Unavailable<% endif %><% endif %> &
<% if risks and risks.air_quality and risks.air_quality.severity %>\textcolor{<< risks.air_quality.severity|sev_color >>}{<< risks.air_quality.severity|sev_label(language) >>}<% else %>\textcolor{MutedText}{<% if language == "nl" %>Niet beoordeeld<% else %>Not assessed<% endif %>}<% endif %> &
<% if risks and risks.air_quality and risks.air_quality.source %><< risks.air_quality.source >><% if risks.air_quality.source_date %> · << risks.air_quality.source_date >><% endif %><% else %><% if language == "nl" %>Geen bron<% else %>No source<% endif %><% endif %> \\

Climate stress &
<% if risks and risks.climate_stress and risks.climate_stress.score is not none %><< risks.climate_stress.score >>/100<% else %><% if language == "nl" %>Niet beschikbaar<% else %>Unavailable<% endif %><% endif %> &
<% if risks and risks.climate_stress and risks.climate_stress.severity %>\textcolor{<< risks.climate_stress.severity|sev_color >>}{<< risks.climate_stress.severity|sev_label(language) >>}<% else %>\textcolor{MutedText}{<% if language == "nl" %>Niet beoordeeld<% else %>Not assessed<% endif %>}<% endif %> &
<% if risks and risks.climate_stress and risks.climate_stress.source %><< risks.climate_stress.source >><% if risks.climate_stress.source_date %> · << risks.climate_stress.source_date >><% endif %><% else %><% if language == "nl" %>Geen bron<% else %>No source<% endif %><% endif %> \\

Sunlight &
<% if sunlight_state == "available" and sunlight_score is not none %><< sunlight_score >>/100<% else %><% if language == "nl" %>Datakloof (zie Zonlichtstatus)<% else %>Data gap (see Sunlight Status)<% endif %><% endif %> &
<% if sunlight_state == "available" and risks and risks.sunlight and risks.sunlight.severity %>\textcolor{<< risks.sunlight.severity|sev_color >>}{<< risks.sunlight.severity|sev_label(language) >>}<% elif sunlight_state == "available" and sunlight_score is not none %>
<% if sunlight_score >= 70 %>\textcolor{sev-good}{<% if language == "nl" %>Goed<% else %>Good<% endif %>}<% elif sunlight_score >= 40 %>\textcolor{sev-mod}{<% if language == "nl" %>Matig<% else %>Moderate<% endif %>}<% elif sunlight_score >= 20 %>\textcolor{sev-poor}{<% if language == "nl" %>Slecht<% else %>Poor<% endif %>}<% else %>\textcolor{sev-crit}{<% if language == "nl" %>Kritiek<% else %>Critical<% endif %>}<% endif %><% else %>\textcolor{MutedText}{<% if language == "nl" %>Niet voltooid<% else %>Not completed<% endif %>}<% endif %> &
SunCalc + 3DBAG \\

<% if tier_b and tier_b.crime and tier_b.crime.score is not none %>
<% if language == "nl" %>Criminaliteit<% else %>Crime<% endif %> &
<< tier_b.crime.score >>/100 &
<% if tier_b.crime.severity %>\textcolor{<< tier_b.crime.severity|sev_color >>}{<< tier_b.crime.severity|sev_label(language) >>}<% else %>\textcolor{MutedText}{<% if language == "nl" %>Niet beoordeeld<% else %>Not assessed<% endif %>}<% endif %> &
<% if tier_b.crime.source %><< tier_b.crime.source >><% if tier_b.crime.source_date %> · << tier_b.crime.source_date >><% elif tier_b.crime.yearly_period %> · << tier_b.crime.yearly_period >><% endif %><% else %><% if language == "nl" %>Geen bron<% else %>No source<% endif %><% endif %> \\
<% endif %>

\bottomrule
\end{longtable}

<% if climate_disclosure %>
{\footnotesize\textcolor{MutedText}{\parbox{\linewidth}{\raggedright\sloppy << climate_disclosure >>}}}\par
<% endif %>

<% if risk_grid_chart %>
\begin{center}
\includegraphics[width=0.92\linewidth]{<< risk_grid_chart >>}
\end{center}
<% endif %>

<% if comparison_chart_blocks %>
\subsection*{<% if language == "nl" %>Vergelijkingsgrafieken<% else %>Comparison Charts<% endif %>}
<% for block in comparison_chart_blocks %>
\Needspace{58mm}
\noindent\includegraphics[width=0.92\linewidth]{<< block.path >>}\par
<% if block.measurement_line %>
{\footnotesize\textbf{<% if language == "nl" %>Metingen<% else %>Measurements<% endif %>}: << block.measurement_line >>}\par
<% endif %>
<% if block.unit_definition %>
{\footnotesize\textcolor{MutedText}{<< block.unit_definition >>}}\par
<% endif %>
\vspace{4pt}
<% endfor %>
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Vergelijkingsbalken gebruiken de buurt-check 0-100 schaal (niet ruwe eenheden). Hoger = beter.<% else %>Comparison bars use the buurt-check 0-100 scale (not raw units). Higher = better.<% endif %>}}\par
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Referentielijnen tonen WHO/target waarden omgerekend naar dezelfde schaal.<% else %>Reference lines show WHO/target values mapped to the same scale.<% endif %>}}
<% endif %>

\section*{<% if language == "nl" %>Zonlichtstatus<% else %>Sunlight Status<% endif %>}
<% if sunlight_state == "available" %>
\textbf{\textcolor{sev-good}{<% if language == "nl" %>Beschikbaar<% else %>Available<% endif %>}}
<% if risks and risks.sunlight %>
\\<% if language == "nl" %>Winter: << ("%.1f"|format(risks.sunlight.winter_hours)).replace(".", ",") if risks.sunlight.winter_hours is not none else "n.b." >> u/dag · Equinox: << ("%.1f"|format(risks.sunlight.equinox_hours)).replace(".", ",") if risks.sunlight.equinox_hours is not none else "n.b." >> u/dag · Zomer: << ("%.1f"|format(risks.sunlight.summer_hours)).replace(".", ",") if risks.sunlight.summer_hours is not none else "n.b." >> u/dag<% else %>Winter: << "%.1f"|format(risks.sunlight.winter_hours) if risks.sunlight.winter_hours is not none else "n/a" >> h/day · Equinox: << "%.1f"|format(risks.sunlight.equinox_hours) if risks.sunlight.equinox_hours is not none else "n/a" >> h/day · Summer: << "%.1f"|format(risks.sunlight.summer_hours) if risks.sunlight.summer_hours is not none else "n/a" >> h/day<% endif %>
<% endif %>
<% elif sunlight_state == "pending" %>
\noindent\fcolorbox{sev-mod}{white}{\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{
\textbf{\textcolor{sev-mod}{<% if language == "nl" %>Datakloof: zonlichtanalyse niet voltooid<% else %>Data gap: sunlight analysis not completed<% endif %>}}\\
<< sunlight_pending_message if sunlight_pending_message else ("Zonlichtanalyse was niet voltooid voor export. Exporteer opnieuw na het bekijken van het 3D-model." if language == "nl" else "Sunlight analysis was not completed before export. Re-export after viewing the 3D model.") >>
}}
<% else %>
\noindent\fcolorbox{sev-mod}{white}{\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{
\textbf{\textcolor{sev-mod}{<% if language == "nl" %>Datakloof: zonlicht ontbreekt<% else %>Data gap: sunlight missing<% endif %>}}\\
<< sunlight_unavailable_message if sunlight_unavailable_message else ("Zonlichtanalyse is niet beschikbaar voor deze export. Exporteer opnieuw nadat je het 3D-model hebt geopend." if language == "nl" else "Sunlight analysis is unavailable for this export. Re-export after opening the 3D model.") >>
}}
<% endif %>

<% if location_map %>
\subsection*{<% if language == "nl" %>Locatiekaart<% else %>Location Map<% endif %>}
\begin{center}
\includegraphics[width=0.78\linewidth]{<< location_map >>}
\end{center}
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Luchtfoto: PDOK Luchtfoto (CC BY 4.0) · context 1 km · noord boven<% else %>Aerial: PDOK Luchtfoto (CC BY 4.0) · 1 km context · north up<% endif %>}}
<% else %>
\subsection*{<% if language == "nl" %>Locatiekaart<% else %>Location Map<% endif %>}
\noindent\fcolorbox{MutedText}{white}{\parbox{\dimexpr0.78\linewidth-2\fboxsep-2\fboxrule\relax}{
\textbf{<% if language == "nl" %>Locatiekaart niet beschikbaar<% else %>Location map unavailable<% endif %>}\\
<% if language == "nl" %>PDOK-luchtfoto kon niet laden tijdens export. Het dossier gaat verder zonder de kaartcontext van 1 km.<% else %>PDOK aerial imagery did not load during export. The dossier continues without the 1 km map context.<% endif %>
}}\par
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Luchtfoto: PDOK Luchtfoto (CC BY 4.0) · niet beschikbaar voor deze export<% else %>Aerial: PDOK Luchtfoto (CC BY 4.0) · unavailable for this export<% endif %>}}
<% endif %>

<% if shadow_images %>
\subsection*{<% if language == "nl" %>Schaduwanalyse<% else %>Shadow Analysis<% endif %>}
<% for img in shadow_images %>
\noindent\includegraphics[width=\linewidth]{<< img >>}
<% if not loop.last %>\vspace{4pt}<% endif %>
<% endfor %>
\\{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Seizoensopnamen op 12:00 lokale tijd. Bron: 3DBAG / TU Delft + SunCalc.<% else %>Seasonal snapshots at 12:00 local time. Source: 3DBAG / TU Delft + SunCalc.<% endif %>}}
<% if shadow_time_labels %>
\\{\footnotesize\textcolor{MutedText}{<< shadow_time_labels|join(" · ") >>}}
<% endif %>
<% endif %>

\section*{<% if language == "nl" %>Buurtcontext<% else %>Neighborhood Context<% endif %>}
<% if neighborhood %>
\begin{itemize}
  \item <% if language == "nl" %>Buurtcode<% else %>Neighborhood code<% endif %>: << neighborhood.buurt_code if neighborhood.buurt_code else ("Onbekend" if language == "nl" else "Unknown") >>
  \item <% if language == "nl" %>Gemeente<% else %>Municipality<% endif %>: << neighborhood.gemeente_name if neighborhood.gemeente_name else ("Onbekend" if language == "nl" else "Unknown") >>
<% if neighborhood_urbanization_label %>
  \item <% if language == "nl" %>Stedelijkheid<% else %>Urbanization<% endif %>: << neighborhood_urbanization_label >>
<% endif %>
\end{itemize}
<% if neighborhood_sections %>
<% for section in neighborhood_sections %>
\textbf{<< section.title >>}\\[-2pt]
\begin{tabular}{p{0.56\linewidth}p{0.35\linewidth}}
<% for row in section.rows %>
<< row.label >> & << row.value >> \\
<% endfor %>
\end{tabular}
\vspace{5pt}
<% endfor %>
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Bron: CBS Wijken \& Buurten 2024 · Q1 = laagste 25\% landelijk, Q4 = hoogste 25\%<% else %>Source: CBS Wijken \& Buurten 2024 · Q1 = bottom 25\% nationally, Q4 = top 25\%<% endif %>}}
<% endif %>
<% else %>
<% if language == "nl" %>Buurtgegevens niet beschikbaar.<% else %>Neighborhood data unavailable.<% endif %>
<% endif %>

<% if age_chart %>
\subsection*{<% if language == "nl" %>Leeftijdsverdeling<% else %>Age Distribution<% endif %>}
\includegraphics[width=0.92\linewidth]{<< age_chart >>}
<% if age_interpretation %>\\{\footnotesize\textcolor{MutedText}{<< age_interpretation >>}}<% endif %>
<% endif %>

<% if livability_chart %>
\subsection*{<% if language == "nl" %>Leefbaarheid<% else %>Livability<% endif %>}
<% if livability and livability.overall_normalized is not none %>
\textbf{<% if language == "nl" %>Leefbaarheidsscore<% else %>Livability score<% endif %>}: << livability.overall_normalized >>/100\\
\textbf{<% if language == "nl" %>Ernst<% else %>Severity<% endif %>}:
<% if livability.overall_normalized >= 70 %>\textcolor{sev-good}{<% if language == "nl" %>Goed<% else %>Good<% endif %>}<% elif livability.overall_normalized >= 40 %>\textcolor{sev-mod}{<% if language == "nl" %>Matig<% else %>Moderate<% endif %>}<% elif livability.overall_normalized >= 20 %>\textcolor{sev-poor}{<% if language == "nl" %>Slecht<% else %>Poor<% endif %>}<% else %>\textcolor{sev-crit}{<% if language == "nl" %>Kritiek<% else %>Critical<% endif %>}<% endif %>\\
<% endif %>
<% if livability and livability.dimensions %>
\textbf{<% if language == "nl" %>Dimensies<% else %>Dimensions<% endif %>}\\
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Score per leefbaarheidsdimensie volgens Leefbaarometer-methodiek.<% else %>Dimension-level scores based on Leefbaarometer methodology.<% endif %>}}\\
<% endif %>
<% if livability_trend_summary %>
\textbf{<% if language == "nl" %>Trend<% else %>Trend<% endif %>}\\
{\footnotesize\textcolor{MutedText}{<< livability_trend_summary >>}}\\
<% endif %>
<% if livability and livability.comparison %>
\textbf{<% if language == "nl" %>Vergelijking<% else %>Comparison<% endif %>}
\begin{itemize}
<% for row in livability.comparison %>
  \item << row.name if row.name else ("Onbekend" if language == "nl" else "Unknown") >>: << row.overall_normalized if row.overall_normalized is not none else ("Niet beschikbaar" if language == "nl" else "Unavailable") >>
<% endfor %>
\end{itemize}
<% endif %>
\includegraphics[width=0.92\linewidth]{<< livability_chart >>}
\\{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Bron: Leefbaarometer<% else %>Source: Leefbaarometer<% endif %>}}
<% endif %>

<% if tier_b and tier_b.crime and tier_b.crime.total_per_1000 is not none %>
\subsection*{<% if language == "nl" %>Criminaliteit<% else %>Crime Rate<% endif %>}
<% if language == "nl" %>
Dit adres: << ("%.1f"|format(tier_b.crime.total_per_1000)).replace(".", ",") >> per 1.000\\
<% if tier_b.crime.national_per_1000 is not none %>Landelijk: << ("%.1f"|format(tier_b.crime.national_per_1000)).replace(".", ",") >> per 1.000\\<% endif %>
<% if tier_b.crime.burglary_per_1000 is not none %>Inbraak: << ("%.1f"|format(tier_b.crime.burglary_per_1000)).replace(".", ",") >>\\<% endif %>
<% if tier_b.crime.violent_per_1000 is not none %>Geweld: << ("%.1f"|format(tier_b.crime.violent_per_1000)).replace(".", ",") >>\\<% endif %>
Score: << tier_b.crime.score if tier_b.crime.score is not none else "Niet beschikbaar" >>/100 (<% if tier_b.crime.severity %><< tier_b.crime.severity|sev_label(language) >><% else %>Niet beoordeeld<% endif %>)\\
<% if tier_b.crime.meaning_nl %><< tier_b.crime.meaning_nl >><% endif %>
<% if tier_b.crime.source_date or tier_b.crime.yearly_period %>\\{\footnotesize\textcolor{MutedText}{Bron: CBS · << tier_b.crime.source_date if tier_b.crime.source_date else tier_b.crime.yearly_period >>}}<% endif %>
<% else %>
This address: << "%.1f"|format(tier_b.crime.total_per_1000) >> per 1,000\\
<% if tier_b.crime.national_per_1000 is not none %>National avg: << "%.1f"|format(tier_b.crime.national_per_1000) >> per 1,000\\<% endif %>
<% if tier_b.crime.burglary_per_1000 is not none %>Burglary: << "%.1f"|format(tier_b.crime.burglary_per_1000) >>\\<% endif %>
<% if tier_b.crime.violent_per_1000 is not none %>Violent: << "%.1f"|format(tier_b.crime.violent_per_1000) >>\\<% endif %>
Score: << tier_b.crime.score if tier_b.crime.score is not none else "Unavailable" >>/100 (<% if tier_b.crime.severity %><< tier_b.crime.severity|sev_label(language) >><% else %>Not assessed<% endif %>)\\
<% if tier_b.crime.meaning_en %><< tier_b.crime.meaning_en >><% endif %>
<% if tier_b.crime.source_date or tier_b.crime.yearly_period %>\\{\footnotesize\textcolor{MutedText}{Source: CBS · << tier_b.crime.source_date if tier_b.crime.source_date else tier_b.crime.yearly_period >>}}<% endif %>
<% endif %>
<% endif %>

\section*{<% if language == "nl" %>Aanvullende vastgoedcontroles<% else %>Additional Property Checks<% endif %>}

% 1) Asbestos
<% if not property_warnings %>
\noindent\fcolorbox{sev-mod}{white}{\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{
\textbf{\textcolor{sev-mod}{- <% if language == "nl" %>Asbestbewustzijn<% else %>Asbestos Awareness<% endif %>}}\\
\textbf{<% if language == "nl" %>Handmatige verificatie nodig<% else %>Manual verification required<% endif %>}\\
<% if language == "nl" %>Asbeststatus niet beschikbaar in de exportketen.<% else %>Asbestos status unavailable in export pipeline.<% endif %>\\
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Bron: BAG-bouwjaarheuristiek<% else %>Source: BAG construction year heuristic<% endif %>}}
}}
<% elif property_warnings.asbestos and property_warnings.asbestos.flagged %>
\noindent\fcolorbox{sev-crit}{white}{\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{
\textbf{\textcolor{sev-crit}{▲ <% if language == "nl" %>Asbestbewustzijn<% else %>Asbestos Awareness<% endif %>}}\\
\textbf{\textcolor{sev-crit}{<% if language == "nl" %>Gemarkeerd - bouwjaar in risicoperiode<% else %>Flagged - built in risk period<% endif %>}}\\
<% if language == "nl" %>Mogelijk asbestrisico op basis van bouwjaar. Vraag een asbestinventarisatie aan voor verbouwing.<% else %>Potential asbestos risk flagged from construction year. Request an asbestos inventory before renovation.<% endif %>\\
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Bron: BAG-bouwjaarheuristiek<% else %>Source: BAG construction year heuristic<% endif %>}}
}}
<% else %>
\noindent\fcolorbox{sev-good}{white}{\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{
\textbf{\textcolor{sev-good}{✓ <% if language == "nl" %>Asbestbewustzijn<% else %>Asbestos Awareness<% endif %>}}\\
<% if language == "nl" %>Geen leeftijdsgebonden asbestsignaal in beschikbare gebouwdata.<% else %>No age-based asbestos flag in available building data.<% endif %>\\
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Bron: BAG-bouwjaarheuristiek<% else %>Source: BAG construction year heuristic<% endif %>}}
}}
<% endif %>
\vspace{4pt}

% 2) Foundation
<% if not property_warnings %>
\noindent\fcolorbox{sev-mod}{white}{\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{
\textbf{\textcolor{sev-mod}{- <% if language == "nl" %>Funderingsrisico<% else %>Foundation Risk<% endif %>}}\\
<% if language == "nl" %>Handmatige verificatie nodig.<% else %>Manual verification required.<% endif %>\\
<% if language == "nl" %>Funderingsrisico niet beschikbaar in de exportketen.<% else %>Foundation risk unavailable in export pipeline.<% endif %>\\
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Bron: BRO bodemdata + Klimaateffectatlas bodemdaling<% else %>Source: BRO soil data + Klimaateffectatlas subsidence<% endif %>}}
}}
<% elif property_warnings.foundation_risk and property_warnings.foundation_risk.level == "high" %>
\noindent\fcolorbox{sev-crit}{white}{\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{
\textbf{\textcolor{sev-crit}{▲ <% if language == "nl" %>Funderingsrisico<% else %>Foundation Risk<% endif %>}}\\
\textbf{\textcolor{sev-crit}{<% if language == "nl" %>Gemarkeerd - hoog risico<% else %>Flagged - high risk<% endif %>}}\\
<% if language == "nl" %>Hoog funderingsrisico vastgesteld.<% else %>High foundation risk identified.<% endif %>
<% if property_warnings.foundation_risk.soil_type is defined and property_warnings.foundation_risk.soil_type %> <% if language == "nl" %>Grondsoort<% else %>Soil type<% endif %>: << property_warnings.foundation_risk.soil_type >>.<% endif %>
<% if property_warnings.foundation_risk.subsidence_rate_mm_per_year is defined and property_warnings.foundation_risk.subsidence_rate_mm_per_year is not none %>
 <% if language == "nl" %>Bodemdalingssnelheid: << ("%.1f"|format(property_warnings.foundation_risk.subsidence_rate_mm_per_year)).replace(".", ",") >> mm/jaar.<% else %>Subsidence rate: << "%.1f"|format(property_warnings.foundation_risk.subsidence_rate_mm_per_year) >> mm/year.<% endif %>
<% endif %>
 <% if language == "nl" %>Laat een funderingsinspectie uitvoeren voor aankoop.<% else %>Commission a foundation inspection before purchase.<% endif %>\\
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Bron: BRO bodemdata + Klimaateffectatlas bodemdaling<% else %>Source: BRO soil data + Klimaateffectatlas subsidence<% endif %>}}
}}
<% elif property_warnings.foundation_risk and property_warnings.foundation_risk.level == "medium" %>
\noindent\fcolorbox{sev-mod}{white}{\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{
\textbf{\textcolor{sev-mod}{- <% if language == "nl" %>Funderingsrisico<% else %>Foundation Risk<% endif %>}}\\
\textbf{\textcolor{sev-mod}{<% if language == "nl" %>Aandacht nodig - matig risico<% else %>Attention needed - moderate risk<% endif %>}}\\
<% if language == "nl" %>Matig funderingsrisico. Overweeg een funderingsonderzoek.<% else %>Moderate foundation risk. Consider a foundation survey.<% endif %>\\
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Bron: BRO bodemdata + Klimaateffectatlas bodemdaling<% else %>Source: BRO soil data + Klimaateffectatlas subsidence<% endif %>}}
}}
<% elif not property_warnings.foundation_risk or property_warnings.foundation_risk.level == "unavailable" %>
\noindent\fcolorbox{sev-mod}{white}{\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{
\textbf{\textcolor{sev-mod}{- <% if language == "nl" %>Funderingsrisico<% else %>Foundation Risk<% endif %>}}\\
<% if language == "nl" %>Handmatige verificatie nodig.<% else %>Manual verification required.<% endif %>\\
<% if language == "nl" %>Funderingsrisico niet beschikbaar in de exportketen.<% else %>Foundation risk unavailable in export pipeline.<% endif %>\\
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Bron: BRO bodemdata + Klimaateffectatlas bodemdaling<% else %>Source: BRO soil data + Klimaateffectatlas subsidence<% endif %>}}
}}
<% else %>
\noindent\fcolorbox{sev-good}{white}{\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{
\textbf{\textcolor{sev-good}{✓ <% if language == "nl" %>Funderingsrisico<% else %>Foundation Risk<% endif %>}}\\
<% if language == "nl" %>Geen funderingsrisicosignaal gedetecteerd op basis van beschikbare gegevens.<% else %>No foundation risk signal detected from available data.<% endif %>\\
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Bron: BRO bodemdata + Klimaateffectatlas bodemdaling<% else %>Source: BRO soil data + Klimaateffectatlas subsidence<% endif %>}}
}}
<% endif %>
\vspace{4pt}

% 3) Ground lease
<% if not property_warnings or not property_warnings.erfpacht %>
\noindent\fcolorbox{sev-mod}{white}{\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{
\textbf{\textcolor{sev-mod}{- <% if language == "nl" %>Erfpacht (grondhuur)<% else %>Ground Lease (Erfpacht)<% endif %>}}\\
<% if language == "nl" %>Erfpachtstatus niet beschikbaar in de exportketen.<% else %>Ground lease status unavailable in export pipeline.<% endif %>\\
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Bron: Gemeentelijke erfpachtlijst<% else %>Source: Municipal ground lease registry<% endif %>}}
}}
<% elif property_warnings.erfpacht.detected %>
\noindent\fcolorbox{sev-mod}{white}{\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{
\textbf{\textcolor{sev-mod}{- <% if language == "nl" %>Erfpacht (grondhuur)<% else %>Ground Lease (Erfpacht)<% endif %>}}\\
\textbf{\textcolor{sev-mod}{<% if language == "nl" %>Aandacht nodig - voorwaarden controleren<% else %>Attention needed - verify lease terms<% endif %>}}\\
<% if language == "nl" %>Erfpacht gedetecteerd. Controleer voorwaarden, canon en einddatum bij de notaris.<% else %>Ground lease detected. Verify lease terms, canon amount, and expiry date with the notary.<% endif %>\\
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Bron: Gemeentelijke erfpachtlijst<% else %>Source: Municipal ground lease registry<% endif %>}}
}}
<% else %>
\noindent\fcolorbox{sev-good}{white}{\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{
\textbf{\textcolor{sev-good}{✓ <% if language == "nl" %>Erfpacht (grondhuur)<% else %>Ground Lease (Erfpacht)<% endif %>}}\\
<% if language == "nl" %>Geen erfpachtsignaal gedetecteerd. Dit pand lijkt op eigen grond te staan.<% else %>No ground lease signal detected. This property appears to be on freehold land.<% endif %>\\
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Bron: Gemeentelijke erfpachtlijst<% else %>Source: Municipal ground lease registry<% endif %>}}
}}
<% endif %>
\vspace{4pt}

% 4) VvE
<% if not property_warnings %>
\noindent\fcolorbox{sev-mod}{white}{\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{
\textbf{\textcolor{sev-mod}{- <% if language == "nl" %>VvE (Vereniging van Eigenaren)<% else %>VvE (Owners\textquotesingle{} Association)<% endif %>}}\\
<% if language == "nl" %>VvE-status niet beschikbaar in de exportketen.<% else %>VvE status unavailable in export pipeline.<% endif %>\\
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Bron: BAG verblijfsobjecten<% else %>Source: BAG dwelling unit count<% endif %>}}
}}
<% elif property_warnings.vve and property_warnings.vve.is_apartment %>
\noindent\fcolorbox{sev-mod}{white}{\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{
\textbf{\textcolor{sev-mod}{- <% if language == "nl" %>VvE (Vereniging van Eigenaren)<% else %>VvE (Owners\textquotesingle{} Association)<% endif %>}}\\
\textbf{\textcolor{sev-mod}{<% if language == "nl" %>Aandacht nodig - documenten opvragen<% else %>Attention needed - request documents<% endif %>}}\\
<% if language == "nl" %>Dit is een appartementsrecht. Vraag VvE-jaarstukken en reservefondsgegevens op.<% else %>This is an apartment right. Request VvE annual documents and reserve fund details.<% endif %>\\
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Bron: BAG verblijfsobjecten<% else %>Source: BAG dwelling unit count<% endif %>}}
}}
<% else %>
\noindent\fcolorbox{sev-good}{white}{\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{
\textbf{\textcolor{sev-good}{✓ <% if language == "nl" %>VvE (Vereniging van Eigenaren)<% else %>VvE (Owners\textquotesingle{} Association)<% endif %>}}\\
<% if language == "nl" %>Geen VvE van toepassing. Dit pand is geen appartementsrecht.<% else %>No owners' association applicable. This property is not an apartment.<% endif %>\\
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Bron: BAG verblijfsobjecten<% else %>Source: BAG dwelling unit count<% endif %>}}
}}
<% endif %>
\vspace{4pt}

% 5) Lead pipe
<% if not property_warnings or not property_warnings.lead_pipe %>
\noindent\fcolorbox{sev-mod}{white}{\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{
\textbf{\textcolor{sev-mod}{- <% if language == "nl" %>Loden leidingen<% else %>Lead Pipe Risk<% endif %>}}\\
<% if language == "nl" %>Loden leidingen status niet beschikbaar in de exportketen.<% else %>Lead pipe status unavailable in export pipeline.<% endif %>\\
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Bron: BAG-bouwjaarheuristiek (pre-1960)<% else %>Source: BAG construction year heuristic (pre-1960)<% endif %>}}
}}
<% elif property_warnings.lead_pipe.flagged %>
\noindent\fcolorbox{sev-crit}{white}{\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{
\textbf{\textcolor{sev-crit}{▲ <% if language == "nl" %>Loden leidingen<% else %>Lead Pipe Risk<% endif %>}}\\
\textbf{\textcolor{sev-crit}{<% if language == "nl" %>Gemarkeerd - bouwjaar verhoogt kans<% else %>Flagged - building age raises probability<% endif %>}}\\
<% if language == "nl" %>Mogelijk loden leidingen op basis van bouwjaar. Laat een watertest uitvoeren.<% else %>Potential lead pipes flagged from construction year. Commission a water test and request utility records.<% endif %>\\
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Bron: BAG-bouwjaarheuristiek (pre-1960)<% else %>Source: BAG construction year heuristic (pre-1960)<% endif %>}}
}}
<% else %>
\noindent\fcolorbox{sev-good}{white}{\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{
\textbf{\textcolor{sev-good}{✓ <% if language == "nl" %>Loden leidingen<% else %>Lead Pipe Risk<% endif %>}}\\
<% if language == "nl" %>Geen signaal voor loden leidingen op basis van beschikbare gebouwdata.<% else %>No lead pipe signal detected from available building data.<% endif %>\\
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Bron: BAG-bouwjaarheuristiek (pre-1960)<% else %>Source: BAG construction year heuristic (pre-1960)<% endif %>}}
}}
<% endif %>
\vspace{4pt}

% 6) Soil contamination
\noindent\fcolorbox{sev-mod}{white}{\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{
\textbf{\textcolor{sev-mod}{- <% if language == "nl" %>Bodemverontreiniging<% else %>Soil Contamination<% endif %>}}\\
\textbf{\textcolor{sev-mod}{<% if language == "nl" %>Handmatige verificatie vereist<% else %>Manual verification required<% endif %>}}\\
<% if language == "nl" %>Er is geen geautomatiseerde perceelgebonden bodemdata beschikbaar. BRO is niet betrouwbaar voor perceelniveau-extractie.<% else %>No automated parcel-level soil contamination data is available. BRO soil registry is not reliable for parcel-level extraction.<% endif %>
<% if postcode %> <% if language == "nl" %>Raadpleeg bodemloket.nl voor postcode << postcode >>.<% else %>Visit bodemloket.nl for postcode << postcode >>.<% endif %><% else %> <% if language == "nl" %>Raadpleeg bodemloket.nl met het volledige adres.<% else %>Visit bodemloket.nl with the full property address.<% endif %><% endif %>\\
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Bron: BRO bodemregister + bodemloket.nl<% else %>Source: BRO soil registry + bodemloket.nl<% endif %>}}
}}
\vspace{4pt}

% 7) Direct sunlight estimate
<% if sunlight_score is not none %>
<% set winter_hours = risks.sunlight.winter_hours|default(none) if risks and risks.sunlight else none %>
<% set equinox_hours = risks.sunlight.equinox_hours|default(none) if risks and risks.sunlight else none %>
<% set summer_hours = risks.sunlight.summer_hours|default(none) if risks and risks.sunlight else none %>
<% set annual_average = risks.sunlight.annual_average|default(none) if risks and risks.sunlight else none %>
<% set svf_anisotropic = risks.sunlight.svf_anisotropic|default(none) if risks and risks.sunlight else none %>
<% set svf_percent = risks.sunlight.svf_percent|default(none) if risks and risks.sunlight else none %>
<% set irradiance_kwh_m2 = risks.sunlight.irradiance_kwh_m2|default(none) if risks and risks.sunlight else none %>
\noindent\fcolorbox{<% if sunlight_score >= 70 %>sev-good<% elif sunlight_score >= 40 %>sev-mod<% elif sunlight_score >= 20 %>sev-poor<% else %>sev-crit<% endif %>}{white}{\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{
\textbf{\textcolor{<% if sunlight_score >= 70 %>sev-good<% elif sunlight_score >= 40 %>sev-mod<% elif sunlight_score >= 20 %>sev-poor<% else %>sev-crit<% endif %>}{<% if sunlight_score >= 70 %>✓<% elif sunlight_score >= 40 %>-<% else %>▲<% endif %> <% if language == "nl" %>Direct zonlicht (helderheidsschatting)<% else %>Direct sun (clear-sky visibility)<% endif %>}}\\
<% if winter_hours is not none or equinox_hours is not none or summer_hours is not none %>
<% if language == "nl" %>Geschat direct zonlicht: winter << ("%.1f"|format(winter_hours)).replace(".", ",") if winter_hours is not none else "—" >>h/dag, equinox << ("%.1f"|format(equinox_hours)).replace(".", ",") if equinox_hours is not none else "—" >>h/dag, zomer << ("%.1f"|format(summer_hours)).replace(".", ",") if summer_hours is not none else "—" >>h/dag. Score: << sunlight_score >>/100.<% else %>Estimated direct sunlight: winter << "%.1f"|format(winter_hours) if winter_hours is not none else "—" >>h/day, equinox << "%.1f"|format(equinox_hours) if equinox_hours is not none else "—" >>h/day, summer << "%.1f"|format(summer_hours) if summer_hours is not none else "—" >>h/day. Score: << sunlight_score >>/100.<% endif %><%
if annual_average is not none
or (svf_anisotropic is not none and svf_anisotropic != svf_percent)
or irradiance_kwh_m2 is not none
%> <% if annual_average is not none %><% if language == "nl" %>Jaargemiddelde: << ("%.1f"|format(annual_average)).replace(".", ",") >> u/dag<% else %>Annual average: << "%.1f"|format(annual_average) >> h/day<% endif %><% endif %><% if svf_anisotropic is not none and svf_anisotropic != svf_percent %><% if annual_average is not none %> | <% endif %><% if language == "nl" %>SVF (anisotropisch): << "%.0f"|format(svf_anisotropic) >>\%<% else %>SVF (anisotropic): << "%.0f"|format(svf_anisotropic) >>\%<% endif %><% endif %><% if irradiance_kwh_m2 is not none %><% if annual_average is not none or (svf_anisotropic is not none and svf_anisotropic != svf_percent) %> | <% endif %><% if language == "nl" %>Zonnestraling: << "%.0f"|format(irradiance_kwh_m2) >> kWh/m²/jaar<% else %>Solar irradiance: << "%.0f"|format(irradiance_kwh_m2) >> kWh/m²/year<% endif %>.<% endif %><% endif %>
<% else %>
<% if language == "nl" %>Geschatte zonlichtscore: << sunlight_score >>/100.<% else %>Estimated direct sunlight score: << sunlight_score >>/100.<% endif %>
<% endif %>\\
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Bron: SunCalc + 3DBAG<% else %>Source: SunCalc + 3DBAG<% endif %>}}
}}
<% else %>
\noindent\fcolorbox{sev-mod}{white}{\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{
\textbf{\textcolor{sev-mod}{- <% if language == "nl" %>Direct zonlicht (helderheidsschatting)<% else %>Direct sun (clear-sky visibility)<% endif %>}}\\
<% if language == "nl" %>Zonlichtanalyse was niet afgerond voor export. Exporteer opnieuw nadat je het 3D-model hebt bekeken.<% else %>Sunlight analysis was not completed before export. Re-export after viewing the 3D model.<% endif %>\\
{\footnotesize\textcolor{MutedText}{<% if language == "nl" %>Bron: SunCalc + 3DBAG<% else %>Source: SunCalc + 3DBAG<% endif %>}}
}}
<% endif %>

\section*{<% if language == "nl" %>Bezichtigingsvragen<% else %>Viewing Questions<% endif %>}
<% if viewing_questions and viewing_questions.categories %>
\begin{itemize}
<% for category in viewing_questions.categories %>
  <% for q in category.questions[:3] %>
  \item <% if language == "nl" %><< q.text_nl >><% else %><< q.text_en >><% endif %>
  <% endfor %>
<% endfor %>
\end{itemize}
<% else %>
<% if language == "nl" %>Geen vragen beschikbaar voor deze export.<% else %>No questions available for this export.<% endif %>
<% endif %>

\section*{<% if language == "nl" %>Methodologie<% else %>Methodology<% endif %>}
<% if methodology %>
<< methodology.intro >>

\subsection*{<< methodology.formula_heading >>}
\begin{itemize}
<% for row in methodology.formulas %>
  \item \textbf{<< row.category >>}: << row.formula >>
<% endfor %>
\end{itemize}

\subsection*{<< methodology.sources_heading >>}
\begin{longtable}{p{0.30\linewidth}p{0.32\linewidth}p{0.33\linewidth}}
\toprule
\textbf{<% if language == "nl" %>Bron<% else %>Source<% endif %>} &
\textbf{<% if language == "nl" %>Datatype<% else %>Data type<% endif %>} &
\textbf{<% if language == "nl" %>Protocol<% else %>Protocol<% endif %>} \\
\midrule
<% for row in methodology.sources %>
<< row.source >> & << row.data_type >> & << row.protocol >> \\
<% endfor %>
\bottomrule
\end{longtable}

\subsection*{<< methodology.sunlight_heading >>}
\begin{itemize}
<% for row in methodology.sunlight_method %>
  \item \textbf{<< row.label >>}: << row.description >>
<% endfor %>
\end{itemize}

{\footnotesize\textcolor{MutedText}{<< methodology.peer_disclosure >>}}
\vspace{4pt}

\textbf{\textcolor{sev-mod}{<< methodology.limitations_heading >>}}\\
<< methodology.limitations >>
<% else %>
<% if language == "nl" %>Hoe we risico's scoren: alle indicatoren worden genormaliseerd naar een 0-100 schaal waarbij hoger beter is.<% else %>How we score risks: indicators are normalized to a 0-100 scale where higher is better.<% endif %>
<% endif %>

\section*{<% if language == "nl" %>Provenance<% else %>Provenance<% endif %>}
<% if provenance %>
\begin{itemize}
  <% if provenance.report_id %>\item Report ID: << provenance.report_id >><% endif %>
  <% if provenance.vbo_id %>\item VBO: << provenance.vbo_id >><% endif %>
  <% if provenance.pand_id %>\item Pand: << provenance.pand_id >><% endif %>
  <% if provenance.buurt_code %>\item Buurt: << provenance.buurt_code >><% endif %>
  <% if provenance.methodology_version %>\item Methodology: << provenance.methodology_version >><% endif %>
\end{itemize}
<% else %>
<% if language == "nl" %>Geen provenance metadata aangeleverd.<% else %>No provenance metadata supplied.<% endif %>
<% endif %>

\Needspace{42mm}
\subsection*{<% if language == "nl" %>Uw notities<% else %>Your viewing notes<% endif %>}
\noindent\rule{\linewidth}{0.1pt}\vspace{8pt}
\noindent\rule{\linewidth}{0.1pt}\vspace{8pt}
\noindent\rule{\linewidth}{0.1pt}\vspace{8pt}
\noindent\rule{\linewidth}{0.1pt}

\end{document}
