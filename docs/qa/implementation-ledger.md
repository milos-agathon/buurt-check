# Implementation Ledger

Date: 2026-05-15

Scope: Phase 3 remediation only. Work was executed in dependency order from `tasks.md` across T-024 through T-033. A final repair pass on 2026-05-15 fixed the remaining Phase 3 blockers before T-034: duplicate queued-run scheduling, queue-first `/run` behavior, confidence thresholds, result-key translation coverage, and lifecycle analytics locale preservation.

| Task | PRD tags | Test / diagnostic first | Implementation completed | Verification command and result |
| --- | --- | --- | --- | --- |
| T-024 | FR-M1, FR-M2; PRD 14.4-14.5; Constitution XIII | Added/ran job tests for slow and expired states. Final repair added regressions for duplicate run requests against the same active queued/running vector and feature-store failure after run creation. Initial final repair suite failed because duplicate requests scheduled two workers and feature-store failure happened before job insert. | Added slow-state marking from status polling, expired-job transition, runtime tracking, retry after expired, stable no-store endpoint errors, created/reused job start metadata, scheduling only for newly created queued jobs, and queue-first job insert using the known `match-seed-v1` data version before worker-side feature loading. | `cd backend && pytest -q tests/test_match_jobs.py` covered in the final focused command; `cd backend && pytest -q tests/test_match_jobs.py tests/test_match_results_contract.py tests/test_match_hard_filters.py tests/test_match_model_honesty.py tests/test_match_instrumentation.py tests/test_match_db_schema.py` passed, 47 tests in 15.90 s. |
| T-025 | FR-M4, FR-M6; PRD 8.7, 15.3; Constitution V/X | Extended results-contract tests for runtime, preference-vector version, fit label key, candidate counts, stable confidence/limitation keys, stretch separation, no predictive probability, PRD confidence boundaries, and EN/NL translation coverage for every emitted `reason_codes` and `tradeoffs` value. Initial final repair suite failed on 20/50/79/80 confidence boundaries and missing `green_access_match` translation coverage. | Added result schema fields, stable public keys, geometry/limitation metadata, separate stretch matches, runtime and count serialization, score-based confidence levels (`0-19`, `20-49`, `50-79`, `80-100`), and namespaced `match.results.reasons.*` / `match.results.tradeoffs.*` keys with EN/NL translations. | Covered by the final focused command: 47 tests passed in 15.90 s. Frontend JSON/build coverage: `cd frontend && npm run build` passed. |
| T-026 | FR-M3, FR-M5; PRD 21.1; Constitution V | Added hard-filter regression proving every matrix candidate receives score/filter status. | Preserved deterministic weighted scoring and separated stretch/failed-hard-filter results in result serialization. | Covered by the final focused command: 47 tests passed in 15.90 s. |
| T-027 | PRD 14.5, 8.7 | Exercised run/status/results response models through job and result contract tests. | Extended Pydantic models for runtime, fit label key, preference vector version, separate stretch matches, and candidate counts. | Covered by T-024/T-025 verification. |
| T-028 | FR-M2, FR-M6; PRD 27.5 | Extended DB schema test for runtime/result metadata columns. | Added idempotent migrations and create-table fields for job runtime, result preference vector version, stretch JSON, and candidate/scored counts. | Covered by the final focused command: 47 tests passed in 15.90 s. |
| T-029 | FR-M3, FR-M4; PRD 15.1, 15.3 | Added feature-matrix source/freshness/limitation assertion. | Added feature-vector freshness and limitation fields populated by the seed importer while preserving the loading abstraction. | Covered by the final focused command: 47 tests passed in 15.90 s. |
| T-030 | PRD 8.6, 27.1; Constitution V/X | Updated model-honesty tests so labels alone are insufficient; labels plus validation evidence are required for predictive candidate mode. | Tightened `ModelSelectionService` to require both labels and evaluation evidence; public result mode remains weighted scoring/no predictive probability. | Backend model-honesty tests were covered by the final focused command: 47 tests passed in 15.90 s. `cd frontend && npm run test -- src/test/match-first-model-honesty.test.ts` passed, 1 test in 1.18 s. |
| T-031 | PRD 14.3; FR-M1 to FR-M7 | Endpoint tests assert stable public errors and `Cache-Control: no-store`. | Updated run/status/results/session endpoint error codes and no-store headers. | Covered by `cd backend && pytest -q tests/test_match_jobs.py tests/test_match_results_contract.py`. |
| T-032 | PRD 20.2, 20.5; Constitution XV | Added instrumentation tests for persistent sanitized lifecycle events. Final repair added a Dutch-session lifecycle regression and initially failed because running/terminal rows defaulted to locale `en`. | Added public instrumentation sink/persistence path and wired job CTA/queued/running/completed/fallback/no-strong/failed/slow events without internal error classes or raw answers. Final repair joins/reads `match_sessions.locale` for running, terminal, slow, and stale-failure events. | `cd backend && pytest -q tests/test_match_instrumentation.py` covered in the final focused command: 47 tests passed in 15.90 s. |
| T-033 | PRD Phase 3; Constitution VIII/XI | Ran Phase 3 targeted backend and frontend guards after code changes. Final repair reconciled the prior broad non-live evidence conflict by rerunning the exact broad backend command. | Updated traceability, latest handoff, open punch list, requirements matrix, and ledger with exact final repair evidence. | `cd backend && ruff check .` passed with `All checks passed!`; `cd backend && pytest -q tests/test_match_jobs.py tests/test_match_results_contract.py tests/test_match_hard_filters.py tests/test_match_model_honesty.py tests/test_match_instrumentation.py tests/test_match_db_schema.py` passed, 47 tests in 15.90 s; `cd backend && python -m pytest -x -q -m "not live" --color=no` passed, 1332 passed, 12 skipped, 11 deselected in 243.34 s (0:04:03); `cd frontend && npm run build` passed; `cd frontend && npm run test -- src/test/match-first-model-honesty.test.ts` passed, 1 test in 1.18 s. |

## Tests Not Run

| Command | Reason | Unverified scope |
| --- | --- | --- |
| `/speckit.analyze` | Not run in this implementation session. | Full cross-artifact feature analysis gate; required before claiming the entire feature complete. |
