# Match-First Revamp Traceability

Date: 2026-05-17

Scope: Match-first revamp phase traceability, including Phase 1/2 closure
evidence, Phase 3 backend 100% closure repair, Phase 4 progress/success UI,
Phase 5 Netherlands results map/list, Phase 6 selected-neighborhood detail
completion, and Phase 7 house-to-Dossier bridge evidence. Phase 7 now includes
provider-backed nearby address selection through the backend PDOK Locatieserver
reverse path plus localized manual recovery.

## CI Repair 2026-05-16

| CI failure | Exact work completed | Files changed | Verification |
| --- | --- | --- | --- |
| PR 29 `Frontend Build + Test` failed because `match-first-copy-guard.test.ts` crossed newlines from TypeScript syntax and falsely treated `MatchingProgressScreen.tsx` type declarations/generic type syntax as visible hard-coded copy. | Restricted the visible-copy regex to same-line JSX-like text, preserving the guard against hard-coded visible match-first copy while avoiding TypeScript declaration false positives. No product behavior changed. | `frontend/src/test/match-first-copy-guard.test.ts`, `docs/ai/latest_handoff.md`, `docs/qa/match_first_revamp_traceability.md` | Reproduced the failing guard locally, then verified `cd frontend && npm run test -- src/test/match-first-copy-guard.test.ts` passed with 7 tests; `cd frontend && npm run test` passed; `cd frontend && npm run build` passed. |

## Phase 1 Acceptance Mapping

| PRD / constitution item | Implementation surface | Acceptance criteria | Verification |
| --- | --- | --- | --- |
| FR-L2, FR-L3; Constitution I | `frontend/src/App.tsx`, `frontend/src/components/match-first/MatchFirstLanding.tsx`, `frontend/src/components/TabBar.tsx` | Root and `#/match` show one dominant match CTA. Address search is secondary on the landing screen, and the main app tab is labeled Match rather than Search. | `frontend/src/App.test.tsx` initial render tests, `frontend/src/components/TabBar.test.tsx` |
| FR-L4; Constitution III, VII | `frontend/src/components/TopBar.tsx`, `frontend/src/components/match-first/MatchFirstLanding.tsx`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json` | Landing has one language switcher, with native button semantics, keyboard operation, and bilingual labels from translation keys. | `MatchFirstLanding.test.tsx`, `keyboard-navigation.test.tsx`, `match-first-copy-guard.test.ts` |
| FR-L5, FR-L6; Constitution IV, VII | `frontend/src/components/match-first/HeroMapBackground.tsx` | Hero respects reduced motion and switches to a static fallback state if the image fails. | `HeroMapBackground.test.tsx` |
| Phase 1 canonical flow; Constitution I | `frontend/src/routing/hashRoutes.ts`, `frontend/src/App.tsx`, `frontend/src/components/match-first/SurveyIntro.tsx` | Landing CTA goes to survey intro before the first survey question. `#/match/intro` is a stable route. | `match-first-routing.test.tsx`, `SurveyIntro.test.tsx` |
| FR-S1, FR-S2, FR-S3, FR-S4, FR-S5; Constitution II, IX | `frontend/src/components/match-first/SurveyShell.tsx`, `frontend/src/components/match-first/SurveyReview.tsx` | Phase 1 dummy survey shell shows one question, progress, required-answer validation, session-scoped local answer persistence, review, back behavior, and completion callback. Final 10-12 question content, backend persistence, and preference-vector generation remain Phase 2. | `SurveyShell.test.tsx`, `SurveyReview.test.tsx` |
| FR-S1; Constitution II | `frontend/src/routing/hashRoutes.ts`, `frontend/src/test/match-first-routing.test.tsx` | Because Phase 1 intentionally has one dummy survey question, direct `#/match/session/{session_id}/question/2` and higher question routes are rejected instead of rendering question 1 with misleading progress. Phase 2 must replace this guard with the real 10-12 question route bounds. | `match-first-routing.test.tsx` |
| Phase 7 preparation; Constitution VI, IX | `frontend/src/routing/hashRoutes.ts`, `frontend/src/App.tsx` | Dossier routes can carry explicit match-return context without reusing checkout `session_id`; structured cold-start `match_context` survives query/hash parsing for selected neighborhood, map center, zoom, mobile map/list mode, selected result ID, selected result rank, selected house, and list scroll; Dossier shows Back to match map whenever that context exists, including loading, empty, and address-unavailable states. | `match-first-routing.test.tsx`, `App.test.tsx` |
| Constitution IX | `frontend/src/App.tsx`, `frontend/src/App.test.tsx` | Match-map context restored by the user's Back to match map action survives a browser refresh on the returned selected-neighborhood route, while arbitrary persisted Dossier return context still cannot unlock a direct neighborhood route. | `App.test.tsx` |
| Constitution V, X | `frontend/src/test/match-first-copy-guard.test.ts` | Match-first copy blocks unsupported model certainty, safety, happiness, investment, future-value, guarantee, and predictive-probability claims before result phases add more copy. | `match-first-copy-guard.test.ts` |
| Constitution I, III | `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json`, `frontend/src/test/match-first-copy-guard.test.ts` | The survey review labels the buy/rent/both answer as a housing goal rather than a search type, keeping review copy match-first and bilingual through translation keys. | `match-first-copy-guard.test.ts`, `match-i18n.test.ts` |
| Constitution VII | `frontend/src/App.tsx` | Hash-route transitions move focus to the main content container so keyboard and screen-reader users are not left on stale controls. | `App.test.tsx`, `keyboard-navigation.test.tsx` |
| Constitution VII | `frontend/src/test/match-first-a11y.test.tsx` | Direct Phase 1 placeholder states for run, results, and selected-neighborhood routes expose headings/actions/status copy without serious accessibility violations. | `match-first-a11y.test.tsx` |

## Phase 1 100% Closure Work

| Gap from review | Exact work required | Files changed | Acceptance criteria | Verification |
| --- | --- | --- | --- | --- |
| Survey route allowed nonexistent questions. | Reject direct Phase 1 survey question routes above step 1 until Phase 2 introduces the real question set and route bounds. | `frontend/src/routing/hashRoutes.ts`, `frontend/src/test/match-first-routing.test.tsx` | `question/1` remains valid; `question/0`, non-numeric, decimal, and `question/2` routes return `not_found`. | `cd frontend && npm run test -- src/test/match-first-routing.test.tsx` |
| Dossier return context could be lost after refreshing the returned match route. | Store a Back-to-match-map-activated return context separately from passive Dossier parsing, and hydrate selected-neighborhood route state from that activated context only. | `frontend/src/App.tsx`, `frontend/src/App.test.tsx` | After clicking Back to match map from Dossier, refresh on `#/match/session/{id}/neighborhood/{neighborhood_id}` restores selected neighborhood, map center, zoom, list scroll, mobile mode, selected result, rank, and selected house. Manually seeded arbitrary localStorage context still does not unlock direct neighborhood routes. | `cd frontend && npm run test -- src/App.test.tsx -- -t "refreshing the selected-neighborhood route"` plus existing direct-route guard tests |
| Review copy used search-first wording. | Change the review answer label from Search type / Zoektype to Housing goal / Woondoel and add a copy guard. | `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json`, `frontend/src/test/match-first-copy-guard.test.ts` | New or changed review copy stays bilingual and does not use search/zoek framing for the match-first survey answer. | `cd frontend && npm run test -- src/test/match-first-copy-guard.test.ts -- -t "search-first framing"` |
| Invalid-route recovery still used hard-coded English fallback strings. | Add explicit EN/NL `notFound.*` translation keys, remove default string arguments from `NotFoundScreen`, and verify invalid Phase 1 match question routes localize in Dutch instead of falling back to English. | `frontend/src/components/NotFoundScreen.tsx`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json`, `frontend/src/App.test.tsx`, `frontend/src/test/match-i18n.test.ts` | `#/match/session/{id}/question/nope` renders localized recovery copy and focused recovery action; `NotFoundScreen` has no user-facing fallback strings; EN/NL key parity includes `notFound.*`. | `cd frontend && npm run test -- src/App.test.tsx src/test/match-i18n.test.ts` |
| Invalid match-route recovery still made Search the focused primary action. | Make `NotFoundScreen` match-route aware and route invalid `#/match/...` recovery back to the same match session survey before offering address search as a secondary action. | `frontend/src/App.tsx`, `frontend/src/components/NotFoundScreen.tsx`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json`, `frontend/src/App.test.tsx`, `frontend/src/test/match-i18n.test.ts` | Invalid `#/match/session/{id}/question/nope` focuses localized Back to survey / Terug naar vragen, keeps Search available as a secondary action, and clicking the focused action navigates to `#/match/session/{id}/question/1`. | `cd frontend && npm run test -- src/App.test.tsx src/test/match-i18n.test.ts -- -t "routes invalid match question|defines Dutch and English"` |
| Direct success route could imply completed matching without backend-backed status. | Replace the generic direct-success recovery rendering with a dedicated neutral, localized success-route placeholder that states completion cannot be confirmed yet. | `frontend/src/App.tsx`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json`, `frontend/src/App.test.tsx` | Direct `#/match/session/{id}/success` does not show "matches are ready" or results-unavailable copy; it shows "Match status unavailable" and a Back to survey action. | `cd frontend && npm run test -- src/App.test.tsx -- -t "neutral success-route placeholder"` |
| Restored selected-neighborhood placeholder lacked a recovery action and implied no neighborhood was selected. | Replace the selected-neighborhood restored placeholder with neutral localized copy and a Back to survey action while preserving context data attributes for Phase 5-7 hydration. | `frontend/src/App.tsx`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json`, `frontend/src/App.test.tsx`, `frontend/src/test/match-i18n.test.ts` | After Dossier Back to match map and refresh on `#/match/session/{id}/neighborhood/{neighborhood_id}`, the restored shell keeps session/neighborhood/map/list/house attributes, does not say "3D houses load only after a neighborhood is selected", and exposes localized Back to survey. | `cd frontend && npm run test -- src/App.test.tsx src/test/match-i18n.test.ts -- -t "restores returned match-map context|defines Dutch and English"` |
| Copy guard missed a Phase 1 route surface. | Include `NotFoundScreen` in match-surface copy scans and block `t(key, "English fallback")` defaults across Phase 1 route surfaces. | `frontend/src/test/match-first-copy-guard.test.ts` | Phase 1 route surfaces fail tests if they introduce hard-coded fallback strings or unlocalized Suspense fallbacks. | `cd frontend && npm run test -- src/test/match-first-copy-guard.test.ts` |
| Direct placeholder route a11y coverage was incomplete. | Extend match-first a11y tests to include direct success and selected-neighborhood placeholder states in addition to run and results placeholders. | `frontend/src/test/match-first-a11y.test.tsx` | Direct `run`, `success`, `results`, and `neighborhood` Phase 1 placeholder states have no serious axe violations. | `cd frontend && npm run test -- src/test/match-first-a11y.test.tsx` |
| App-driven hash navigation could ignore the next real hash change, and browser Back/Forward did not reapply hash routes. | Remove the stale app-driven hash-ignore guard and route both `hashchange` and `popstate` through the same parsed-route application path. | `frontend/src/App.tsx`, `frontend/src/App.test.tsx` | After the match CTA pushes the survey intro route, a user hash change to `#/search` renders address search. A browser history `popstate` to `#/match/session/{id}/question/1` renders the one-question survey shell. | `cd frontend && npm run test -- src/App.test.tsx -- -t "applies user hash changes|applies browser back and forward"` |
| Loaded Dossier search exits could drop match-return context. | Add a shared search-route builder that preserves the current or pending `MatchReturnContext`, then use it for Dossier change-address and next-step search exits plus saved/compare search entry points. | `frontend/src/App.tsx`, `frontend/src/App.test.tsx` | Clicking Change address from a loaded match-return Dossier lands on `#/search?...` with `match_return`, `match_session`, and `match_context` still present so the next Dossier can still show Back to match map. | `cd frontend && npm run test -- src/App.test.tsx -- -t "preserves match-return context when changing address"` |
| Phase 1 completion needed explicit proof. | Run the Phase 1 targeted Vitest suite, route-context regressions, strict frontend build, and match-first E2E shell checks after the fixes. | No production file beyond rows above. | Phase 1 reaches 100% of its documented shell/route-cleanup acceptance scope. Later PRD work remains explicitly Phase 2-7. | `cd frontend && npm run test -- src/test/match-first-copy-guard.test.ts src/test/match-i18n.test.ts src/App.test.tsx src/test/match-first-a11y.test.tsx`; `cd frontend && npm run build`; `cd frontend && npm run test:e2e -- tests/e2e/match-first-phase1.spec.ts` |

Closure status after 2026-05-14 verification: **100% of the documented Phase 1 UI shell and route-cleanup closure scope**. This does not reclassify Phase 2-7 PRD work as complete.

## Phase 2 100% Closure Work

| Gap from review | Exact work required | Files changed | Acceptance criteria | Verification |
| --- | --- | --- | --- | --- |
| Frontend created local match session IDs instead of server-issued sessions. | Replace local-only session creation on the landing/intro/survey/review path with `POST /api/match/sessions`, store the returned `session_id`, and keep direct session routes compatible. | `frontend/src/App.tsx`, `frontend/src/components/match-first/MatchFirstLanding.tsx`, `frontend/src/components/match-first/SurveyIntro.tsx`, `frontend/src/services/matchFirstApi.ts`, `frontend/src/App.test.tsx` | Clicking the match CTA calls `POST /api/match/sessions`, routes to `#/match/session/{server_id}/intro`, and subsequent survey answer saves patch that server-issued session. | `cd frontend && npm run test -- src/App.test.tsx -- -t "creates a backend match session"` |
| Review/run could proceed from locally saved answers even if backend persistence or vector creation failed. | Require a backend-complete session with a current `preference_vector` before routing from review to run; surface localized recovery when sync/vector readback fails. | `frontend/src/App.tsx`, `frontend/src/components/match-first/SurveyReview.tsx`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json`, `frontend/src/App.test.tsx`, `frontend/src/components/match-first/SurveyReview.test.tsx` | `Show my matches` does not start the run shell until `GET /api/match/sessions/{session_id}` returns `is_complete: true` and `preference_vector`; failed readback keeps the user on review with an accessible localized alert and retry path. | `cd frontend && npm run test -- src/App.test.tsx src/components/match-first/SurveyReview.test.tsx -- -t "requires backend vector"` |
| Rent/both users could not provide rent budget even though the vector supports `rent_max`. | Make budget capture intent-aware, add localized monthly rent max input, and preserve `rent_max` in frontend/backend vector contracts. | `frontend/src/components/match-first/BudgetRangeQuestion.tsx`, `frontend/src/components/match-first/SurveyShell.tsx`, `frontend/src/components/match-first/SurveyShell.test.tsx`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json`, `backend/tests/test_match_preference_vector_builder.py` | Rent-only answers can complete the survey with monthly rent max and produce a vector with `monthly_rent_max_cents`; buy-only users still see buy min/max. | `cd frontend && npm run test -- src/test/match-first-survey.test.tsx src/components/match-first/SurveyShell.test.tsx`; `cd backend && pytest -q tests/test_match_preference_vector_builder.py` |
| Intent and budget could drift apart after switching buy/rent/both, creating impossible hard filters. | Make frontend and backend budget validation intent-aware; prune stale budget fields when intent changes; build vectors only with budget dimensions that apply to the chosen intent. | `frontend/src/components/match-first/SurveyShell.tsx`, `frontend/src/components/match-first/BudgetRangeQuestion.tsx`, `frontend/src/components/match-first/surveyValidation.ts`, `frontend/src/components/match-first/SurveyShell.test.tsx`, `backend/app/services/match/survey_schema.py`, `backend/app/services/match/preference_vector.py`, `backend/tests/test_match_preference_vector_builder.py`, `backend/tests/test_match_sessions.py` | Buy requires a valid buy range, rent requires a valid monthly rent cap, and both requires both. Switching intent removes stale incompatible budget values from stored survey answers and the preference vector. | `cd frontend && npm run test -- src/components/match-first/SurveyShell.test.tsx`; `cd backend && pytest -q tests/test_match_preference_vector_builder.py tests/test_match_sessions.py` |
| Review completeness used only `undefined` checks, allowing invalid persisted answers to reach the final CTA. | Reuse a shared frontend survey validator for review, missing-answer state, and question advancement. | `frontend/src/components/match-first/surveyValidation.ts`, `frontend/src/components/match-first/SurveyShell.tsx`, `frontend/src/components/match-first/SurveyReview.tsx`, `frontend/src/components/match-first/SurveyReview.test.tsx` | Empty arrays, blank anchors, invalid budgets, and missing required answers render the localized review recovery instead of the final run CTA. | `cd frontend && npm run test -- src/components/match-first/SurveyReview.test.tsx` |
| Review could display raw invalid stable values from corrupted local persistence. | Validate single- and multi-select answers against the survey option sets before review, and avoid raw answer-value fallbacks for visible review labels. | `frontend/src/components/match-first/surveyValidation.ts`, `frontend/src/components/match-first/SurveyReview.tsx`, `frontend/src/components/match-first/SurveyReview.test.tsx` | Corrupted values such as translated labels, unknown option IDs, or stale priority IDs recover to the localized review alert instead of showing raw user-facing strings or opening the run CTA. | `cd frontend && npm run test -- src/components/match-first/SurveyReview.test.tsx` |
| Backend accepted protected-trait keys into persisted raw answer JSON. | Reject protected-trait answer keys at PATCH time instead of silently storing them and excluding them only during vector derivation. | `backend/app/services/match/survey_schema.py`, `backend/app/services/match/sessions.py`, `backend/tests/test_match_sessions.py`, `backend/tests/test_match_preference_vector_builder.py` | PATCH payloads containing `nationality`, `ethnicity`, `religion`, `race`, or `immigration_status` return stable error code `match.warning.protected_answer_not_allowed` and are not stored. | `cd backend && pytest -q tests/test_match_sessions.py tests/test_match_preference_vector_builder.py` |
| Max-three multi-select state disabled extra choices without an accessible explanation. | Add localized cap-reached helper/live text and associate it with the multi-select fieldset when the cap is reached. | `frontend/src/components/match-first/MultiSelectQuestion.tsx`, `frontend/src/components/match-first/SurveyShell.css`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json`, `frontend/src/test/match-first-a11y.test.tsx` | After selecting three lifestyle priorities, users get a perceivable localized "maximum selected" message; axe reports no serious violations. | `cd frontend && npm run test -- src/test/match-first-a11y.test.tsx src/test/match-i18n.test.ts` |
| Phase 2 constants were duplicated as hard-coded `11` values across frontend and backend request models. | Centralize frontend review counts on `MATCH_FIRST_SURVEY_QUESTION_COUNT` and backend step limits on the shared backend survey question count. | `frontend/src/components/match-first/SurveyReview.tsx`, `backend/app/services/match/survey_schema.py`, `backend/app/models/match.py`, `frontend/src/test/match-first-routing.test.tsx`, `backend/tests/test_match_sessions.py` | Route bounds, review progress, analytics total steps, and backend `current_step` validation all use the survey question count constant. | `cd frontend && npm run test -- src/test/match-first-routing.test.tsx src/components/match-first/SurveyReview.test.tsx`; `cd backend && pytest -q tests/test_match_sessions.py` |
| Completed sessions only rebuilt vectors from raw answers on readback; they did not persist the preference-vector record. | Persist the generated vector into `match_preference_vectors`, including source answer version, vector version, raw answer refs, warnings, timestamps, and JSON fields, and migrate existing local/Turso schemas safely. | `backend/app/db.py`, `backend/app/services/match/sessions.py`, `backend/tests/test_match_db_schema.py`, `backend/tests/test_match_sessions.py` | Completing the survey stores a durable preference-vector row whose IDs and version match the session response; repeated `init_db` calls add missing metadata columns without data loss. | `cd backend && pytest -q tests/test_match_db_schema.py tests/test_match_sessions.py` |
| Stored local session IDs were reused without server readback, so an expired/deleted session could keep users in a broken flow. | Validate a stored/active match session with `GET /api/match/sessions/{session_id}` before reusing it from landing or intro; create a fresh backend session when readback fails. | `frontend/src/App.tsx`, `frontend/src/App.test.tsx` | A stale locally stored session returning 404 is not reused; the app posts a new session, stores it, and routes to the fresh server-issued intro/question route. | `cd frontend && npm run test -- src/App.test.tsx -- -t "replaces a stale stored backend match session"` |
| Phase 2 copy still contained search-first wording and an over-strong "perfect" match failure phrase. | Rewrite affected survey/result translation values to match-first, non-predictive language and extend the copy guard to scan survey question/helper copy for search-first and certainty terms. | `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json`, `frontend/src/test/match-first-copy-guard.test.ts` | Survey copy no longer frames the user as searching inside the match-first flow, and failure copy avoids perfect-fit language or predictive certainty. | `cd frontend && npm run test -- src/test/match-first-copy-guard.test.ts src/test/match-i18n.test.ts` |
| Backend direct PATCH still contradicted the intent/budget pruning closure requirement. | Normalize merged survey answers on every PATCH so stale buy-only or rent-only budget fields are removed before validation, persistence, and vector creation. | `backend/app/services/match/survey_schema.py`, `backend/app/services/match/sessions.py`, `backend/tests/test_match_sessions.py` | Changing a completed buy session to rent via direct PATCH removes the stored buy budget, marks the session incomplete until rent max is supplied, and clears the vector preview. Rent/both vector budgets still keep only dimensions allowed by intent. | `cd backend && pytest -q tests/test_match_sessions.py tests/test_match_preference_vector_builder.py` |
| Backend could store over-limit multi-select payloads as invalid raw answers instead of rejecting them. | Treat `match.warning.too_many_answers` as a PATCH-level payload error and prevent the invalid array from being stored. | `backend/app/services/match/survey_schema.py`, `backend/tests/test_match_sessions.py` | PATCH payloads with more than three lifestyle priorities return stable error code `match.warning.too_many_answers`; readback confirms the invalid answer was not stored. | `cd backend && pytest -q tests/test_match_sessions.py` |
| Direct survey question URLs could skip unanswered required questions. | Add a shared frontend survey-progress helper and redirect direct question routes to the earliest incomplete required step for the active session snapshot. | `frontend/src/components/match-first/surveyValidation.ts`, `frontend/src/App.tsx`, `frontend/src/App.test.tsx` | Opening `#/match/session/{id}/question/8` with no saved answers redirects/renders question 1. Opening it with only intent answered redirects/renders question 2. Completed local snapshots may still open later question routes. | `cd frontend && npm run test -- src/App.test.tsx -- -t "redirects direct survey question"` |
| A11Y coverage did not prove the whole survey can be completed without a mouse. | Add a keyboard-only survey completion regression that covers radio, number inputs, text input, slider, checkbox groups, final language choice, review CTA, and focus-safe progression. | `frontend/src/test/match-first-a11y.test.tsx` | The full one-question-at-a-time survey reaches the run handoff using only keyboard input and preserves stable answers without pointer interaction. | `cd frontend && npm run test -- src/test/match-first-a11y.test.tsx -- -t "completes the full survey with keyboard"` |
| Review could start a match run from a stale backend vector that no longer matched the displayed answers. | Compare the displayed review answers to `preference_vector.raw_answer_refs` after backend readback and block the run if they differ. | `frontend/src/App.tsx`, `frontend/src/App.test.tsx` | `Show my matches` stays on review with a localized accessible sync alert when the backend vector was built from different answers than the review summary. | `cd frontend && npm run test -- src/App.test.tsx -- -t "blocks review completion"` |
| Sessionless legacy survey URLs could render the survey without a server-issued match session. | Bootstrap `#/match/survey` and `#/match/quiz` through `POST /api/match/sessions` with `source: "resume"` and replace the route with the session-scoped question URL before answers are accepted. | `frontend/src/App.tsx`, `frontend/src/App.test.tsx` | Direct legacy survey and quiz routes render question 1 only after a backend session exists, then route to `#/match/session/{server_id}/question/1`. | `cd frontend && npm run test -- src/App.test.tsx -- -t "creates a backend session before rendering"` |
| Survey question advancement and final review handoff could ignore failed backend answer persistence. | Surface a localized survey sync failure alert and keep the user on the current question until the latest answer save succeeds. | `frontend/src/components/match-first/SurveyShell.tsx`, `frontend/src/components/match-first/SurveyQuestionScreen.tsx`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json`, `frontend/src/components/match-first/SurveyShell.test.tsx` | Failed PATCH requests block Next/review handoff, preserve the visible answer, and expose `matchFirst.survey.syncFailed` through `role="alert"`. | `cd frontend && npm run test -- src/components/match-first/SurveyShell.test.tsx -- -t "backend answer sync fails|final backend answer sync fails"` |
| Review summary was too thin to preserve the user’s preference context before matching. | Expand the review from three rows to a concise 5-8 item summary covering housing goal, budget, anchor, commute, priorities, must-haves, housing type, and area character, all via translation keys. | `frontend/src/components/match-first/SurveyReview.tsx`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json`, `frontend/src/components/match-first/SurveyReview.test.tsx` | Review shows the main constraints users just entered without raw option IDs or hard-coded labels, staying within the PRD’s 5-8 indicator guidance. | `cd frontend && npm run test -- src/components/match-first/SurveyReview.test.tsx -- -t "restores the saved answer set"` |
| Frontend/backend survey counts could still drift because the count literal remained separate from the ordered question list. | Derive frontend and backend question-count constants from their shared ordered question arrays. | `frontend/src/components/match-first/surveyQuestions.ts`, `backend/app/services/match/survey_constants.py`, `backend/app/services/match/survey_schema.py`, `backend/app/models/match.py`, `backend/tests/test_match_survey_schema.py` | Adding or removing a survey question changes route bounds, review progress, and backend step validation through one ordered source instead of a duplicated count. | `cd backend && pytest -q tests/test_match_survey_schema.py`; `cd frontend && npm run test -- src/test/match-first-routing.test.tsx src/components/match-first/SurveyReview.test.tsx` |
| Stored backend session answers were not hydrated into the browser survey snapshot after `sessionStorage` loss. | Hydrate valid `GET /api/match/sessions/{session_id}` responses into `matchSessionStorage`, and let intro Start resume to the earliest incomplete step or review when the backend session is already complete. | `frontend/src/App.tsx`, `frontend/src/App.test.tsx` | A valid stored server session restores raw answers, locale, answer version, and stale-results state. Completed sessions resume to review; stale/deleted sessions still create a fresh backend session. | `cd frontend && npm run test -- src/App.test.tsx -- -t "hydrates stored backend session answers|replaces a stale stored backend match session"` |
| Survey step changes did not move keyboard/screen-reader focus to the new one-question screen. | Focus the active survey question heading whenever `currentStep` changes while preserving the existing validation-alert focus path. | `frontend/src/components/match-first/SurveyQuestionScreen.tsx`, `frontend/src/components/match-first/SurveyShell.test.tsx` | Next and Back move focus to the active question heading; required-answer validation still moves focus to the alert. | `cd frontend && npm run test -- src/components/match-first/SurveyShell.test.tsx -- -t "moves focus to the active question heading|moves focus to the validation message"` |
| Backend warning codes from answer PATCH failures were collapsed into generic sync-failure copy. | Preserve string `detail` values from non-OK match-first API responses, map `match.warning.*` backend codes to translation keys, and keep generic sync copy for network/unrecognized failures. | `frontend/src/services/matchFirstApi.ts`, `frontend/src/components/match-first/SurveyShell.tsx`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json`, `frontend/src/services/matchFirstApi.test.ts`, `frontend/src/components/match-first/SurveyShell.test.tsx`, `frontend/src/test/match-i18n.test.ts` | A backend 422 with `match.warning.invalid_answer_value` displays localized survey guidance; EN/NL key parity covers all backend survey warning codes. | `cd frontend && npm run test -- src/services/matchFirstApi.test.ts src/components/match-first/SurveyShell.test.tsx src/test/match-i18n.test.ts -- -t "surfaces stable backend warning codes|defines Dutch and English"` |
| Frontend/backend survey option contracts could still drift even after count derivation. | Add a backend-owned multi-select limit constant and a parity regression that compares backend question order, required set, option values, and max-selection limits against the frontend survey config. | `backend/app/services/match/survey_schema.py`, `backend/tests/test_match_survey_schema.py`, `frontend/src/components/match-first/surveyQuestions.ts` | Backend validation and frontend question config stay in lockstep for option IDs and max-selection behavior; future drift fails the backend schema test. | `cd backend && pytest -q tests/test_match_survey_schema.py` |
| Phase 2 completion lacked full proof against the Phase 1 closure standard. | Run targeted Phase 2 frontend/backend tests, copy/i18n/a11y guards, strict frontend build, backend lint, and non-live backend tests touched by Phase 2. | No production file beyond rows above. | Phase 2 reaches 100% of its documented survey/preference-vector scope: server-issued session, one-question survey, persistence, validation, backend vector preview, bilingual UI, accessible controls, model-honest copy, no search competition, and no new map/Dossier regressions. | `cd frontend && npm run test -- src/test/match-first-survey.test.tsx src/components/match-first/SurveyShell.test.tsx src/components/match-first/SurveyReview.test.tsx src/services/matchFirstApi.test.ts src/services/matchSessionStorage.test.ts src/test/match-i18n.test.ts src/test/match-first-copy-guard.test.ts src/test/match-first-a11y.test.tsx src/App.test.tsx`; `cd frontend && npm run test -- src/test/match-first-routing.test.tsx`; `cd frontend && npm run build`; `cd backend && ruff check app/services/match app/api/match.py app/models/match.py tests/test_match_sessions.py tests/test_match_preference_vector_builder.py tests/test_match_db_schema.py tests/test_match_survey_schema.py`; `cd backend && pytest -q tests/test_match_sessions.py tests/test_match_preference_vector_builder.py tests/test_match_db_schema.py tests/test_match_survey_schema.py` |

Closure status after 2026-05-14 remediation verification: **100% of the documented Phase 2 survey and preference-vector closure scope**. The final remediation closed backend intent/budget pruning, backend over-limit multi-select persistence, direct survey-question route skipping, stale vector readback, sessionless legacy survey routes, backend save failure handling, richer review context, derived question counts, full keyboard-completion proof, backend session hydration, per-question focus management, backend warning-code surfacing, and frontend/backend survey-contract parity gaps. Verified with the full Phase 2 frontend survey/review/API/storage/i18n/copy/a11y/App/routing suite, strict frontend build, backend ruff, and backend session/vector/schema tests listed above. Later phases still need real async matching completion, results hydration, selected-neighborhood 3D map/detail hydration, and production run-status/result contracts.

## Phase 3 Matching Backend Closure Work

| PRD / constitution item | Implementation surface | Acceptance criteria | Verification |
| --- | --- | --- | --- |
| FR-M1; Constitution XIII | `backend/app/api/match.py`, `backend/app/models/match.py`, `backend/app/services/match/jobs.py`, `backend/tests/test_match_jobs.py` | `POST /api/match/sessions/{session_id}/run` starts only when the request includes `source: "review_final_cta"` and the submitted `preference_vector_version` matches the current backend vector. Non-review and stale-vector requests return stable errors without creating jobs. | `cd backend && pytest -q tests/test_match_jobs.py` |
| Phase 3 API contract; T-031 | `backend/app/api/match.py`, `backend/tests/test_match_jobs.py`, `specs/002-match-first-revamp/contracts/match-first-api.md` | Session/run/status/results endpoints return contract stable codes such as `match.session.not_found`, `match.job.not_found`, `match.results.not_ready`, and `match.results.not_found`, and all Phase 3 session/run/status/results responses include `Cache-Control: no-store`. | `cd backend && pytest -q tests/test_match_jobs.py` |
| FR-M2; PRD Sections 14.4-14.5 | `backend/app/services/match/jobs.py`, `backend/app/db.py`, `backend/tests/test_match_jobs.py`, `backend/tests/test_match_db_schema.py` | Match jobs are persisted, pollable, idempotent for retry of the same vector, and separate lifecycle `status` from progress `stage`. In-progress jobs expose `status="running"` with the current stage/message key; concurrent duplicate run requests against the same active queued/running vector reuse the same `job_id`, keep one `active_job_id`, schedule one background execution, and persist one result set. `/run` inserts the queued job before feature-store loading; a later feature-store failure becomes a pollable failed job. Jobs older than 10,000 ms report `matching_slow` once while preserving the same job and current stage. Stale active jobs are recovered through the endpoint before retry; expired jobs can be rerun from the final review CTA without duplicating active jobs. | `cd backend && pytest -q tests/test_match_jobs.py tests/test_match_db_schema.py`; final focused Phase 3 command passed with 50 tests. |
| FR-M3, FR-M5; PRD Section 15.1 | `backend/app/services/match/neighborhood_features.py`, `backend/app/services/match/scoring.py`, `backend/app/services/match/recommendations.py`, `backend/tests/test_match_hard_filters.py`, `backend/tests/test_match_results_contract.py` | Neighborhood feature matrix loading is abstracted, deterministic scoring uses preference-vector weights, and hard filters for intent, budget, commute, and must-haves are applied from feature-matrix signals. Every feature-matrix candidate receives score/filter status, failed hard-filter neighborhoods are separated from normal ranked results, and stretch matches are structurally separate. | `cd backend && pytest -q tests/test_match_hard_filters.py tests/test_match_results_contract.py` |
| FR-M4, FR-M6; Constitution V/X | `backend/app/models/match.py`, `backend/app/services/match/results.py`, `backend/tests/test_match_results_contract.py`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json` | Results include `session_id`, `status`, `model_mode`, `model_version`, `data_version`, `preference_vector_version`, `runtime_ms`, ranked results, separate stretch matches, candidate/scored counts, `fit_score`, `fit_label_key`, namespaced stable reason codes, namespaced stable tradeoffs, 0-100 confidence with PRD thresholds (`0-19=insufficient`, `20-49=low`, `50-79=medium`, `80-100=high`), geometry references, `map_center`, `bbox`, and `source_metadata` for every ranked/stretch/near-miss source ref with stable source IDs, source type/name keys, metric keys, measurement/retrieved dates where available, freshness status, confidence, and limitation keys covered by EN/NL translations. Predictive probability remains unavailable for MVP. | `cd backend && pytest -q tests/test_match_results_contract.py`; final focused Phase 3 command passed with 50 tests; `cd frontend && npm run build`; `cd frontend && npm run test -- src/test/match-first-model-honesty.test.ts` |
| FR-M7; PRD Sections 8.6 and 27.1 | `backend/app/services/match/model_selection.py`, `backend/app/services/match/jobs.py`, `backend/tests/test_match_model_honesty.py`, `frontend/src/test/match-first-model-honesty.test.ts` | Predictive mode is only a guarded stub when validation labels and evaluation evidence exist. Without labels, model mode is `weighted_scoring` with `not_validated_no_labels`; labels without validation evidence still stay weighted and non-predictive. If the advanced path fails after a deterministic baseline succeeds, the job/result status is `completed_with_fallback` and public responses do not expose raw technical model logs. | `cd backend && pytest -q tests/test_match_model_honesty.py`; `cd frontend && npm run test -- src/test/match-first-model-honesty.test.ts` |
| PRD Sections 20.2 and 20.5; Constitution XV | `backend/app/models/match.py`, `backend/app/services/match/instrumentation.py`, `backend/app/services/match/jobs.py`, `backend/tests/test_match_instrumentation.py` | Backend match lifecycle analytics persist stable event keys in `match_analytics_events` for final run CTA, queued/running/completed/failed/fallback/no-strong/slow job states with sanitized context, no translated labels, no raw answers, no exact anchors/protected traits, no internal exception class exposure, and the persisted lifecycle locale read from `match_sessions.locale`. Concurrent duplicate runs emit one queued event for the one created job. | `cd backend && pytest -q tests/test_match_instrumentation.py`; final focused Phase 3 command passed with 50 tests. |

## Phase 3 100% Closure Work

| Gap from review | Exact work required | Files changed | Acceptance criteria | Verification |
| --- | --- | --- | --- | --- |
| `/run` idempotency was covered only by sequential duplicate tests, leaving a real active-job race. | Add a concurrent endpoint test that forces two final-review run requests past the active-job read, then protect non-terminal jobs with a database uniqueness strategy and contention recovery. | `backend/app/db.py`, `backend/app/services/match/jobs.py`, `backend/tests/test_match_jobs.py`, `backend/tests/test_match_db_schema.py` | Two concurrent requests for the same completed session/vector return the same `job_id`, persist exactly one `match_jobs` row, keep one `active_job_id`, schedule one background job, and produce one result set when the scheduled job runs. | Red-first focused test initially failed with two different `job_id` values. Final: `cd backend && pytest -q tests/test_match_jobs.py ... tests/test_match_db_schema.py` passed with 50 tests. |
| Stale running-job retry was manual/operator-only, not true through `POST /run`. | Run `recover_stale_jobs(..., session_id=session_id)` inside `start_match_job` before active-job reuse, and add an endpoint test for an old active running job retried from `review_final_cta`. | `backend/app/services/match/jobs.py`, `backend/tests/test_match_jobs.py` | The stale job becomes terminal `failed` with `match.warning.retryable_stale_job`; the endpoint starts a new active job only after that terminal transition. | Red-first focused test initially failed by reusing the stale job. Final: same 50-test focused backend command passed. No Phase 3 stale retry behavior remains manual/operator-only. |
| Result evidence exposed `source_refs` but not enough source/freshness metadata for the future UI. | Add `MatchResultSourceMetadata`, serialize source metadata from the feature matrix into every recommendation group, and assert metadata for ranked, stretch, and near-miss results. | `backend/app/models/match.py`, `backend/app/services/match/results.py`, `backend/tests/test_match_results_contract.py`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json` | Every result source ref has metadata with stable source ID, source type/name key, metric key, measurement/retrieved dates where available, freshness status, confidence, and limitation keys with EN/NL translations. | Red-first metadata test initially failed because `ranked_results` lacked `source_metadata`. Final: same 50-test focused backend command passed; frontend build and model-honesty test passed. |
| Worktree had unrelated governance/template/spec/docs changes mixed with Phase 3 backend repair work. | Document the Phase 3 repair file set separately from the existing dirty governance/spec changes before Phase 4. | `docs/ai/latest_handoff.md`, `docs/qa/match_first_revamp_traceability.md`, `docs/qa/open_punchlist.md` | Phase 3 closure files are listed separately, and unrelated dirty changes must be split or separately committed before Phase 4 starts. | `git status --short` reviewed after verification; unrelated `.specify/*`, `AGENTS.md`, `CLAUDE.md`, `docs/context/*`, and broad SpecKit artifact changes remain documented outside the Phase 3 repair set. |
| Phase gate review found analytics-name drift and stale alternate-feature references before Phase 4. | Reconcile the canonical Phase 3 job analytics names in backend/docs, remove old backend job alias events from the allowlist, remove stale alternate-feature artifact references from active handoff context, and rerun the read-only analyze gate. Local active plan/tasks cleanup remains unstaged because those files are part of broader uncommitted SpecKit artifact changes. | `backend/app/models/match.py`, `backend/app/services/match/instrumentation.py`, `backend/tests/test_match_instrumentation.py`, `docs/ai/latest_handoff.md`, `docs/qa/open_punchlist.md` | Canonical Phase 3 job events are `match_final_run_cta_clicked`, `match_job_queued`, `match_job_running`, `match_job_completed`, `match_job_failed`, `match_job_completed_with_fallback`, `match_job_completed_no_strong_matches`, and `match_job_slow`; old aliases are not accepted unless intentionally reintroduced with tests. `.specify/feature.json` and prerequisites resolve to `specs/002-match-first-revamp`. | `cd backend && pytest -q tests/test_match_instrumentation.py`; read-only analyze equivalent resolved `FEATURE_DIR` to `specs/002-match-first-revamp` and reported no critical/high Phase 3 gate blockers. |
| Phase 3 closure needed fresh proof after the review fixes. | Rerun the requested backend and frontend gates after the race, stale retry, and source metadata fixes. | No additional product files beyond the rows above. | Phase 3 backend repair is green without claiming Phase 4-7 work complete. | `ruff check .` passed; focused backend suite passed with 50 tests; non-live backend suite passed with 1335 passed, 12 skipped, 11 deselected; frontend build passed; frontend model-honesty test passed. |

Closure status after 2026-05-16 100% repair and phase-gate verification: **Phase 3 matching backend is verified for the documented backend scope and the specific review blockers are closed. Phase 4 progress/success UI may now consume the repaired backend contract, but Phase 4-7 remain unimplemented.** Red-first evidence: concurrent `/run` initially produced two different jobs, stale retry initially reused the stale running job, and result metadata initially lacked `source_metadata`. Final verified commands: `cd backend && ruff check .` passed with `All checks passed!`; `cd backend && pytest -q tests/test_match_jobs.py tests/test_match_results_contract.py tests/test_match_hard_filters.py tests/test_match_model_honesty.py tests/test_match_instrumentation.py tests/test_match_db_schema.py` passed with 50 tests in 17.78 s; `cd backend && python -m pytest -x -q -m "not live" --color=no` previously passed with 1335 passed, 12 skipped, 11 deselected in 246.11 s (0:04:06); `cd frontend && npm run build` previously passed with Vite client built in 15.53 s and service worker built in 706 ms; `cd frontend && npm run test -- src/test/match-first-model-honesty.test.ts` passed with 1 test in 1.23 s. Read-only analyze equivalent reran and found no critical/high Phase 3 gate blockers.

## Phase 4 Progress And Success States

| PRD / constitution item | Implementation surface | Acceptance criteria | Verification |
| --- | --- | --- | --- |
| FR-M1; PRD Sections 14.3-14.4; Constitution XIII | `frontend/src/App.tsx`, `frontend/src/services/matchFirstApi.ts`, `frontend/src/types/matchFirst.ts`, `frontend/src/App.test.tsx`, `frontend/src/services/matchFirstApi.test.ts` | The review CTA calls `POST /api/match/sessions/{session_id}/run` only after backend vector readback, sends `source: "review_final_cta"` and the current `preference_vector_version`, stores the public job status, and routes to `#/match/session/{id}/run` without loading the legacy map. | `cd frontend && npm run test -- src/test/match-first-progress.test.tsx src/services/matchFirstApi.test.ts src/App.test.tsx` |
| PRD Sections 7 Phase 4, 14.4-14.6, 21.3-21.4; Constitution III/VII/XIII | `frontend/src/components/match-first/MatchingProgressScreen.tsx`, `frontend/src/components/match-first/MatchingProgressScreen.css`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json`, `frontend/src/test/match-first-progress.test.tsx` | Progress renders one friendly bilingual status at a time, polls `GET /status`, respects `poll_after_ms`, exposes an accessible progress/status region, and does not display raw `job_id`, backend stages, error codes, stack traces, model logs, or polling internals to users. | `cd frontend && npm run test -- src/test/match-first-progress.test.tsx src/test/match-i18n.test.ts`; `cd frontend && npx eslint src/components/match-first/MatchingProgressScreen.tsx` |
| PRD AC16-AC18; Sections 14.5-14.6; Constitution V/XV | `frontend/src/components/match-first/MatchingProgressScreen.tsx`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json`, `frontend/src/test/match-first-progress.test.tsx` | Slow backend (`matching_slow`) continues polling with calm copy; failed/expired/cancelled states show localized retry and back-to-survey recovery; `completed_with_fallback` and `completed_no_strong_matches` are usable terminal states with honest localized copy and no predictive certainty claims. | `cd frontend && npm run test -- src/test/match-first-progress.test.tsx src/test/match-i18n.test.ts` |
| PRD AC9; Section 17.4; A11Y-2 | `frontend/src/components/match-first/MatchSuccessCheckmark.tsx`, `frontend/src/components/match-first/MatchSuccessCheckmark.css`, `frontend/src/components/match-first/MatchSuccessCheckmark.test.tsx` | Verified terminal completion shows a large Buurt Check checkmark, animated draw variant, reduced-motion static variant, accessible text equivalent, no confetti, and fallback/no-strong-match copy when applicable. | `cd frontend && npm run test -- src/components/match-first/MatchSuccessCheckmark.test.tsx src/test/match-i18n.test.ts` |
| PRD Sections 6.1 and 24 AC7-AC9; Constitution IX/XIII | `frontend/src/App.tsx`, `frontend/src/components/match-first/MatchingProgressScreen.tsx`, `frontend/src/test/match-first-progress.test.tsx`, `frontend/src/App.test.tsx` | Success and results routes cannot be unlocked by localStorage alone. The app fetches `GET /results` before showing success, validates terminal result identity including a present terminal `result_set_id` and exact `results.result_set_id` equality, shows Results unavailable when result hydration is stale, unavailable, or missing terminal result identity, then transitions by CTA to the verified results route. The Netherlands map/list surface is implemented in the separate Phase 5 slice below. | `cd frontend && npm run test -- src/test/match-first-progress.test.tsx src/App.test.tsx`; 2026-05-16 audit repair final command passed. |
| PRD Sections 20.2, 21.1, 21.3, 21.4; Constitution XV | `frontend/src/services/matchFirstAnalytics.ts`, `frontend/src/services/matchFirstAnalytics.test.ts` | Frontend progress/success analytics use stable event names for final run CTA, queued/running/slow/completed/failed/fallback/no-strong-match, retry, results-unavailable, checkmark, and results-open states, with sanitized context and no translated labels or raw answers. | `cd frontend && npm run test -- src/services/matchFirstAnalytics.test.ts`; `cd frontend && npx eslint src/services/matchFirstAnalytics.ts` |
| Constitution III/VII; PRD A11Y-1 to A11Y-5 | `frontend/src/test/match-first-a11y.test.tsx`, `frontend/src/test/match-i18n.test.ts`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json` | Phase 4 user-facing text exists in Dutch and English. Component-level axe coverage now renders real `MatchingProgressScreen` running, failed retry, and results-unavailable states, plus `MatchSuccessCheckmark` animated and reduced-motion states. Existing survey/route tests cover keyboard and focus behavior outside this component slice; this row does not claim a full browser touch-target or end-to-end focus audit for every Phase 4 path. | `cd frontend && npm run test -- src/test/match-first-a11y.test.tsx src/test/match-i18n.test.ts`; 2026-05-16 audit repair final command passed. |

## Phase 4 100% Closure Work

Completion after review repair and audit repair: **100% (9/9 Phase 4 closure rows complete)** for the documented Phase 4 scope. Phase 5 results map/list is closed separately for its documented slice. Phase 6 selected-neighborhood detail/3D and Phase 7 Dossier bridge remain outside this closure.

| Gap from review | Exact work required | Files changed | Acceptance criteria | Verification |
| --- | --- | --- | --- | --- |
| Old run route was a local placeholder and did not consume Phase 3 backend status. | Replace the placeholder with typed run/status/results API helpers and a polling progress screen that starts from the final review CTA. | `frontend/src/App.tsx`, `frontend/src/services/matchFirstApi.ts`, `frontend/src/types/matchFirst.ts`, `frontend/src/components/match-first/MatchingProgressScreen.tsx`, `frontend/src/test/match-first-progress.test.tsx`, `frontend/src/services/matchFirstApi.test.ts` | Review completion starts backend matching, the run route renders backend-backed progress, polling follows `poll_after_ms`, and terminal success first verifies results. | Red-first Phase 4 tests initially failed on missing helpers/components. Final targeted Phase 4/App command passed. |
| Slow, failed, and fallback states needed user-facing recovery without raw internals. | Add explicit localized states for `matching_slow`, failed/expired/cancelled, `completed_with_fallback`, and `completed_no_strong_matches`, plus retry/back actions and raw-internal guards in tests. | `frontend/src/components/match-first/MatchingProgressScreen.tsx`, `frontend/src/components/match-first/MatchSuccessCheckmark.tsx`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json`, `frontend/src/test/match-first-progress.test.tsx` | Slow keeps polling; failure lets users retry the same session; fallback/no-strong-match terminal states are honest and usable; visible UI does not show `job_id`, backend stage strings, stable error codes, tracebacks, or model logs. | `cd frontend && npm run test -- src/test/match-first-progress.test.tsx src/test/match-i18n.test.ts` passed. |
| Completion needed the branded checkmark and reduced-motion variant. | Add `MatchSuccessCheckmark` and CSS animation with static reduced-motion branch and accessible SVG label. | `frontend/src/components/match-first/MatchSuccessCheckmark.tsx`, `frontend/src/components/match-first/MatchSuccessCheckmark.css`, `frontend/src/components/match-first/MatchSuccessCheckmark.test.tsx` | Completion shows the large Buurt Check checkmark only after verified terminal backend results; reduced-motion mode disables draw animation while preserving the signal. | `cd frontend && npm run test -- src/components/match-first/MatchSuccessCheckmark.test.tsx` passed. |
| Route security had to prevent local completion claims. | Gate success/results routes on in-memory verified status plus fetched results and keep direct/restored terminal-looking routes neutral. | `frontend/src/App.tsx`, `frontend/src/App.test.tsx`, `frontend/src/test/match-first-progress.test.tsx` | LocalStorage alone cannot unlock success/results. Verified completion shows success, then the CTA opens only a placeholder results route. | `cd frontend && npm run test -- src/App.test.tsx src/test/match-first-progress.test.tsx` passed. |
| Terminal results verification could still fall back to a generic 100% progress state if `GET /results` failed or returned stale/mismatched payloads. | Add a distinct results-unavailable recovery state and keep the checkmark locked until `GET /results` returns matching `session_id`, `job_id`, `status`, and `result_set_id` for the terminal job. | `frontend/src/components/match-first/MatchingProgressScreen.tsx`, `frontend/src/test/match-first-progress.test.tsx`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json` | Failed or stale result hydration shows localized Results unavailable copy, exposes retry plus Back to survey, emits no checkmark, and can retry result verification successfully without rerunning the survey. | Red-first repair command failed on the missing distinct recovery. Final: `cd frontend && npm run test -- src/test/match-first-progress.test.tsx src/services/matchFirstAnalytics.test.ts src/test/match-i18n.test.ts` passed with 31 tests. |
| Terminal result gating still allowed completion when terminal status omitted `result_set_id`. | Require terminal `status.result_set_id` to be present and exactly equal to `results.result_set_id` before `onComplete`, and record `match_results_unavailable` with reason `missing_result_set_id` when the terminal identifier is null or missing. | `frontend/src/components/match-first/MatchingProgressScreen.tsx`, `frontend/src/test/match-first-progress.test.tsx` | A terminal status with null or missing `result_set_id` cannot unlock the checkmark even when `GET /results` otherwise matches `session_id`, `job_id`, and `status`; the user sees Results unavailable, `onComplete` is not called, and sanitized analytics records the unavailable result. | Red-first: `cd frontend && npm run test -- src/test/match-first-progress.test.tsx` failed with 2 null/missing `result_set_id` failures before the fix. Final: the same command passed with 24 tests; the broader requested Phase 4 command passed with 55 tests. |
| Phase 4 accessibility evidence over-relied on route placeholders. | Add axe coverage for real `MatchingProgressScreen` running, failed retry, and results-unavailable states, plus `MatchSuccessCheckmark` animated and reduced-motion states. | `frontend/src/test/match-first-a11y.test.tsx` | Real Phase 4 progress and success components have no serious axe violations in those states. Focus/touch-target behavior is not overclaimed from this evidence and remains a browser-level residual check where not directly tested. | `cd frontend && npm run test -- src/test/match-first-a11y.test.tsx` passed with 19 tests; broader requested Phase 4 command passed with 55 tests. |
| Phase 4 needed privacy-safe frontend lifecycle analytics. | Extend the frontend analytics allowlist and tests for progress/success/results-unavailable events while keeping sanitized context only. | `frontend/src/services/matchFirstAnalytics.ts`, `frontend/src/services/matchFirstAnalytics.test.ts` | Event names align with the Phase 3 backend/spec set; context permits stable status/runtime IDs but drops translated labels/free text. `match_results_unavailable` is recorded for stale or unavailable terminal result hydration. | `cd frontend && npm run test -- src/services/matchFirstAnalytics.test.ts`; `cd frontend && npx eslint src/services/matchFirstAnalytics.ts` passed. |
| Phase 4 needed closure proof before Phase 5/6. | Run targeted frontend tests, touched-file lint, frontend build, and document residual risks. | `specs/002-match-first-revamp/tasks.md`, `docs/ai/latest_handoff.md`, `docs/qa/match_first_revamp_traceability.md` | `T-034` through `T-042` are marked complete. Later Phase 5 and Phase 6 closure is recorded in their own sections below. | Earlier Phase 4 commands passed; audit repair final commands also passed: `cd frontend && npm run test -- src/test/match-first-progress.test.tsx src/test/match-first-a11y.test.tsx src/components/match-first/MatchSuccessCheckmark.test.tsx src/services/matchFirstAnalytics.test.ts src/test/match-i18n.test.ts` with 55 tests; targeted ESLint for the touched Phase 4 files; `cd frontend && npm run build`. |

Closure status after 2026-05-16 review repair and audit repair verification: **Phase 4 progress and success states are 100% complete for the documented scope.** The flow now runs final matching through the backend, polls status, handles slow/failed/expired/cancelled/fallback/no-strong-match public states, validates terminal result payload identity before success including present/exact `result_set_id`, shows a distinct localized results-unavailable recovery when result hydration fails or terminal result identity is missing, shows the reduced-motion-aware branded checkmark only after verified results, and transitions by CTA to the results route. Phase 6 selected-neighborhood 3D/detail and Phase 7 Dossier bridge were outside this Phase 4 closure and are covered in later sections. Full repo lint was attempted previously and still has pre-existing files outside the Phase 4 touched surface; touched Phase 4 lint targets passed. Phase 4 a11y evidence is component-level axe plus existing keyboard/focus coverage, not a full browser touch-target audit.

## Phase 5 Netherlands Results Map

| PRD / constitution item | Implementation surface | Acceptance criteria | Verification |
| --- | --- | --- | --- |
| PRD FR-R1; Sections 14.3 and 24 AC10; Constitution XIII/XIV | `frontend/src/App.tsx`, `frontend/src/components/match-first/ResultsMap.tsx`, `frontend/src/types/matchFirst.ts`, `frontend/src/test/match-first-results-map.test.tsx` | `#/match/session/{session_id}/results` hydrates a completed session through `GET /api/match/sessions/{session_id}/results`, renders verified result payloads, and does not call `/run` for an existing completed session. | `cd frontend && npm run test -- src/test/match-first-results-map.test.tsx`; broader focused command including `src/App.test.tsx` and `src/services/matchFirstApi.test.ts` passed. |
| PRD FR-R1, FR-R3, FR-R4, FR-R7; Section 16.2; Constitution IV/XII | `frontend/package.json`, `frontend/package-lock.json`, `specs/002-match-first-revamp/implementation-notes.md`, `frontend/src/components/match-first/ResultsMap.tsx`, `frontend/src/components/match-first/ResultsMap.css` | Leaflet is added only for the lazy 2D results map. The map starts at the Netherlands center from the result payload or fallback `[52.2, 5.3]`, uses local markers/polygons from result geometry, supports manual pan/zoom, and does not load 3D buildings, external tiles, or national-zoom amenities. | `cd frontend && npm run build` passed; targeted ESLint for new map files passed. Build output showed the lazy `ResultsMap` JS chunk at 159.82 kB / 46.10 kB gzip and CSS at 20.75 kB / 7.88 kB gzip. |
| PRD FR-R2, FR-R5, FR-R6; Constitution V/X | `frontend/src/components/match-first/RecommendationList.tsx`, `frontend/src/components/match-first/RecommendationCard.tsx`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json`, `frontend/src/test/match-first-results-map.test.tsx`, `frontend/src/test/match-first-copy-guard.test.ts` | Ranked list shows recommendation rank, name, municipality, score/fit label, confidence summary, and at most two translated reason lines. It avoids unsupported certainty, safety, happiness, investment, future-value, guarantee, and predictive-probability claims. Expandable details were intentionally left out because this Phase 5 request required concise translated reason lines only. | `cd frontend && npm run test -- src/test/match-first-results-map.test.tsx src/test/match-first-copy-guard.test.ts`; broader focused command passed. |
| PRD FR-R2 to FR-R7; A11Y-6; Constitution VII/IX | `frontend/src/components/match-first/ResultsMap.tsx`, `frontend/src/components/match-first/RecommendationList.tsx`, `frontend/src/services/matchSessionStorage.ts`, `frontend/src/test/match-first-results-map.test.tsx`, `frontend/src/test/match-first-a11y.test.tsx` | List click moves the map and highlights the selected neighborhood; marker/polygon click highlights the matching list item; mobile users can switch Map/List modes; recommendations remain usable from the list without any map interaction. Selected recommendation, map center/zoom, rank, mobile mode, locale, result set, vector version, and list scroll are persisted for later Dossier return. | `cd frontend && npm run test -- src/test/match-first-results-map.test.tsx` covered sync, mobile toggle, empty/no-strong-match, and state preservation; `cd frontend && npm run test -- src/test/match-first-a11y.test.tsx` passed for the Phase 5 slice, and the expanded current a11y suite passes with 19 tests in the Phase 4 audit repair. |
| PRD Sections 20.3, 21, 15.3; Constitution XV | `frontend/src/services/matchFirstAnalytics.ts`, `frontend/src/services/matchFirstAnalytics.test.ts`, `frontend/src/components/match-first/ResultsMap.tsx` | Results-map analytics use stable allowlisted event names for results open, sufficient confidence, list selection, map feature selection, and map-layer failure. Context uses stable IDs, rank, zoom/mode, and confidence metadata without translated labels or raw answers. | `cd frontend && npm run test -- src/services/matchFirstAnalytics.test.ts`; broader focused command passed. |
| PRD AC16-AC18 and no-strong-match honesty; Constitution V/XV | `frontend/src/components/match-first/ResultsMap.tsx`, `frontend/src/components/match-first/RecommendationList.tsx`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json`, `frontend/src/test/match-first-results-map.test.tsx` | `completed_no_strong_matches` renders honest possible-match copy, keeps near-miss/stretch recommendations usable from the list, and does not imply perfect fit or guaranteed outcomes. | `cd frontend && npm run test -- src/test/match-first-results-map.test.tsx` passed. |

## Phase 5 Closure Work

Completion after implementation: **100% (10/10 Phase 5 task rows marked complete for the requested Phase 5 slice)**. Browser-level e2e/performance proof remains a documented residual verification gap because the selected-neighborhood/house route is still Phase 6/7 scope.

| Gap / requirement | Exact work completed | Files changed | Acceptance criteria | Verification |
| --- | --- | --- | --- | --- |
| Results route still showed a placeholder after Phase 4. | Replaced the verified results placeholder with a lazy `ResultsMap` route that fetches completed results when no verified in-memory payload exists, while keeping direct unverified routes neutral. | `frontend/src/App.tsx`, `frontend/src/components/match-first/ResultsMap.tsx`, `frontend/src/test/match-first-results-map.test.tsx` | Opening an existing completed session results route loads results and does not rerun matching. | Red-first test initially failed because `ResultsMap` did not exist. Final focused map test passed. |
| Existing map surfaces could not provide Phase 5 pan/zoom/vector/list synchronization. | Added `leaflet` plus `@types/leaflet`, documented the dependency decision, and kept the map tile-free and Phase-5-scoped. | `frontend/package.json`, `frontend/package-lock.json`, `specs/002-match-first-revamp/implementation-notes.md` | The dependency is justified; build succeeds; no external tile URL, 3D building, or national amenity loading is introduced. | `cd frontend && npm run build` passed; targeted ESLint for new files passed. |
| Result data was still typed as loose records. | Added typed recommendation, confidence, geometry, source metadata, map payload, and persisted map-state contracts. | `frontend/src/types/matchFirst.ts`, `frontend/src/services/matchSessionStorage.ts` | Components consume typed completed result payloads and store stable return-state keys. | `cd frontend && npm run build` passed. |
| Ranked recommendations needed concise, translated display. | Added recommendation list/card components with rank, fit score/label, municipality, confidence summary, and max two translated reason lines. | `frontend/src/components/match-first/RecommendationList.tsx`, `frontend/src/components/match-first/RecommendationCard.tsx`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json`, `frontend/src/test/match-i18n.test.ts` | No hard-coded user-facing match-first strings; EN/NL key parity covers the new results copy. | `cd frontend && npm run test -- src/test/match-i18n.test.ts src/test/match-first-copy-guard.test.ts` passed as part of the focused command. |
| List/map selection and mobile usability were missing. | Added synchronized selected state, marker buttons, Leaflet vector click handlers, mobile Map/List toggle, keyboard-selectable cards, and a list-first fallback. | `frontend/src/components/match-first/ResultsMap.tsx`, `frontend/src/components/match-first/ResultsMap.css`, `frontend/src/test/match-first-results-map.test.tsx` | List click flies/sets the map to the result and highlights it; marker/polygon click highlights the card; mobile toggle switches visible panel; list remains usable without map. | `cd frontend && npm run test -- src/test/match-first-results-map.test.tsx` passed with sync/mobile/no-strong/state tests. |
| Later Dossier return needs stable map state, but Phase 7 bridge is not in scope. | Persisted selected recommendation/neighborhood/rank, result set, vector version, map center/zoom, list scroll, mobile mode, and locale to sessionStorage. Repaired cold completed-results routes so fetched result payloads restore saved map/list state only when `resultSetId` and `preferenceVectorVersion` match. | `frontend/src/services/matchSessionStorage.ts`, `frontend/src/components/match-first/ResultsMap.tsx`, `frontend/src/test/match-first-results-map.test.tsx` | State is stored for later Dossier return without implementing house click or Dossier route changes. Reopening a completed results route preserves saved selected view, zoom, mobile mode, and selected recommendation instead of resetting to the national default. | Red-first: `cd frontend && npm run test -- src/test/match-first-results-map.test.tsx -- -t "restores saved map view"` failed with `data-map-center="52.2,5.3"` instead of `52.1,5.03`; after the fix the same command passed. Full map-state test suite passed with 5 tests. |
| Results analytics needed stable event names. | Extended the frontend analytics allowlist for results open, sufficient confidence, recommendation selected, map feature selected, and map layer failed. | `frontend/src/services/matchFirstAnalytics.ts`, `frontend/src/services/matchFirstAnalytics.test.ts` | Events keep stable IDs and sanitized context only. | `cd frontend && npm run test -- src/services/matchFirstAnalytics.test.ts` passed as part of the focused command. |
| Phase 5 needed closure proof and next-step handoff. | Updated task status, traceability, implementation notes, and latest handoff with files changed, commands, performance notes, fallbacks, and residual risks. | `specs/002-match-first-revamp/tasks.md`, `specs/002-match-first-revamp/implementation-notes.md`, `docs/qa/match_first_revamp_traceability.md`, `docs/ai/latest_handoff.md` | Phase 6 is clearly the next implementation step, and Phase 5 does not claim 3D/detail/Dossier bridge completion. | This document and `docs/ai/latest_handoff.md` updated. |

## Phase 5 Review Repair 2026-05-16

| Gap / requirement | Exact work completed | Files changed | Acceptance criteria | Verification |
| --- | --- | --- | --- | --- |
| Stale restored map state could bypass the required Netherlands start when success opened results from verified in-memory results after a rerun. | Validated saved map state against active `initialResults.result_set_id` and `preference_vector_version` before using selected recommendation, mobile mode, map center, or zoom. Also applied the same guard during Leaflet initialization and reset stale fetched-route mobile mode to Map. | `frontend/src/components/match-first/ResultsMap.tsx`, `frontend/src/test/match-first-results-map.test.tsx` | A new result set or vector version starts at the Netherlands center, national zoom, Map mode, and no preselected recommendation even when `sessionStorage` contains a previous selected view for the same session. | Red-first: `cd frontend && npm run test -- src/test/match-first-results-map.test.tsx` failed with `data-map-center="52.1,5.03"` instead of `52.2,5.3`; final command passed with 8 tests. |
| Map-origin selection highlighted the matching card but could leave it out of view in a longer scrollable list. | Added a selected list-row marker and scroll the selected recommendation row into view after marker or polygon selection. List-origin selection remains unchanged. | `frontend/src/components/match-first/ResultsMap.tsx`, `frontend/src/components/match-first/RecommendationList.tsx`, `frontend/src/test/match-first-results-map.test.tsx` | Clicking a marker or polygon selects the recommendation and visibly reveals the matching list item through `scrollIntoView` where the browser supports it. | Red-first: map-origin selection did not call `scrollIntoView`; final map test suite passed. |
| List scroll state was persisted only when another tracked state changed after scrolling. | Added an `onScroll` handler to `RecommendationList` and save `listScroll` directly from the scroll event. | `frontend/src/components/match-first/ResultsMap.tsx`, `frontend/src/components/match-first/RecommendationList.tsx`, `frontend/src/test/match-first-results-map.test.tsx` | Scrolling the ranked list updates the saved map state immediately, so later Dossier-return restoration can use the user's final list position. | Red-first: scroll event persisted `listScroll: 0` instead of `144`; final map test suite passed. |
| Phase 5 review repair needed closure proof without claiming Phase 6/7 work. | Ran SpecKit prerequisites/checklist validation, targeted map tests, related a11y/i18n/copy tests, touched-file ESLint, broader Phase 5-focused Vitest, frontend build, and updated handoff/traceability. | `docs/ai/latest_handoff.md`, `docs/qa/match_first_revamp_traceability.md` | Phase 5 findings are closed for this map/list slice. Selected-neighborhood detail/3D, house click, and Dossier bridge remain later phases. | Commands listed below passed; broader Vitest still prints existing Dossier/3D console output and React act warnings from `App.test.tsx`, with no failed tests. |

Final verified commands for Phase 5:

- `.specify/scripts/powershell/check-prerequisites.ps1 -Json -RequireTasks -IncludeTasks` returned `FEATURE_DIR` as `C:\Users\milos\buurt-check\specs\002-match-first-revamp` with `research.md`, `data-model.md`, `contracts/`, `quickstart.md`, and `tasks.md` available.
- Checklist status: `requirements.md` passed with 76/76 completed items.
- `cd frontend && npm run test -- src/test/match-first-results-map.test.tsx` first failed for the three review regressions, then passed with 8 tests after the repair.
- `cd frontend && npm run test -- src/test/match-first-results-map.test.tsx -- -t "restores saved map view"` first failed for the restored cold-route state regression, then passed after the repair.
- `cd frontend && npm run test -- src/test/match-first-a11y.test.tsx src/test/match-first-copy-guard.test.ts src/test/match-i18n.test.ts` passed with 28 tests.
- `cd frontend && npm run test -- src/test/match-first-results-map.test.tsx src/test/match-first-progress.test.tsx src/App.test.tsx src/services/matchFirstApi.test.ts src/services/matchFirstAnalytics.test.ts src/test/match-i18n.test.ts src/test/match-first-copy-guard.test.ts` passed. Existing noisy Dossier/3D console output and React act warnings still print from older tests, but no tests failed.
- `cd frontend && npm run test -- src/test/match-first-a11y.test.tsx` passed for the Phase 5 slice; the expanded current suite passes with 19 tests in the Phase 4 audit repair.
- `cd frontend && npx eslint src/components/match-first/ResultsMap.tsx src/components/match-first/RecommendationList.tsx src/test/match-first-results-map.test.tsx` passed for the review repair.
- `cd frontend && npx eslint src/components/match-first/ResultsMap.tsx src/test/match-first-results-map.test.tsx` passed for the repair.
- `cd frontend && npm exec -- eslint src/components/match-first/ResultsMap.tsx src/components/match-first/RecommendationCard.tsx src/components/match-first/RecommendationList.tsx src/test/match-first-results-map.test.tsx` passed in the original Phase 5 closure.
- `cd frontend && npm run build` passed; the review-repair build emitted `ResultsMap-aGJKtWIH.js` at 160.78 kB / 46.41 kB gzip and `ResultsMap-CoY3xfYW.css` at 20.75 kB / 7.88 kB gzip.

Residual Phase 5 risks:

- Full `cd frontend && npm run lint` still fails on pre-existing non-Phase-5 files; targeted lint for the new Phase 5 files passed.
- No Playwright e2e/browser performance test was added in this slice. Build chunk evidence and targeted Vitest coverage exist, but browser-level map profiling remains open before production release.
- Phase 7 must connect selected house/building context to the existing Dossier return flow.
- `npm install` reported 15 audit vulnerabilities after adding Leaflet dependencies; remediation was outside this Phase 5 scope.

## Phase 6 Selected-Neighborhood Detail

Completion after implementation: **complete for the requested Phase 6 slice**. The
selected-neighborhood route/state now opens from a result recommendation, loads
completed results without rerunning matching, requests selected-neighborhood
geometry/layers/amenities/buildings only after selection, validates building
RD New bounds server-side, renders a nonblank 2D fallback when seed 3D is
missing, and leaves Phase 7 Dossier bridge out of scope.

| PRD / constitution item | Implementation surface | Acceptance criteria | Verification |
| --- | --- | --- | --- |
| PRD FR-N1, FR-N2, FR-N5, FR-N6; Sections 14.3 and 16.3; Constitution IV/XIII | `backend/app/api/match.py`, `backend/app/models/match.py`, `backend/app/services/match/geometry.py`, `backend/app/services/match/buildings.py`, `backend/tests/test_match_neighborhood_layers.py` | `/api/match/neighborhoods/{id}`, `/map-layers`, and `/buildings` expose selected-neighborhood-only geometry. Building requests require `session_id`, `result_set_id`, `bounds_rd`, `lod`, and `limit`; national/out-of-bounds RD requests return stable `match.building_bounds_out_of_scope`. | `cd backend && pytest -q tests/test_match_neighborhood_layers.py` passed with 4 tests; `cd backend && ruff check app tests/test_match_neighborhood_layers.py` passed. |
| PRD FR-N3; Section 16.4; Constitution V/X | `backend/app/services/match/amenities.py`, `backend/app/models/match.py`, `frontend/src/components/match-first/AmenityTags.tsx`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json`, `frontend/src/test/match-first-neighborhood-detail.test.tsx` | Amenity tags are preference-aware from stored session answers, use stable translated label/reason keys, are capped to 5-7 default-visible categories, and do not return national/all-amenity clutter. | Backend amenity cap/relevance test passed in `test_match_neighborhood_layers.py`; frontend detail test verifies 7 rendered tags and empty map points; i18n/copy guards passed. |
| PRD FR-N1, FR-N2, FR-N5; A11Y-6; Constitution VII/IX | `frontend/src/App.tsx`, `frontend/src/components/match-first/ResultsMap.tsx`, `frontend/src/components/match-first/RecommendationCard.tsx`, `frontend/src/components/match-first/RecommendationList.tsx`, `frontend/src/components/match-first/NeighborhoodDetail.tsx`, `frontend/src/components/match-first/NeighborhoodBuildingLayer.tsx`, `frontend/src/test/match-first-neighborhood-detail.test.tsx`, `frontend/src/test/match-first-results-map.test.tsx` | Result-card "View neighborhood" opens `#/match/session/{session_id}/neighborhood/{id}` state. The detail view shows selected boundary, fit context, concise translated reason lines, 2D missing-3D fallback, and stores selected-neighborhood map state for later return. Building fetches occur only after layer bounds are loaded for the selected neighborhood. | `cd frontend && npm run test -- src/test/match-first-neighborhood-detail.test.tsx src/test/match-first-results-map.test.tsx src/services/matchFirstApi.test.ts` passed with 23 tests. |
| PRD FR-N4, FR-N6; Phase 7 boundary; Constitution VI | `frontend/src/components/match-first/HouseSelectionPanel.tsx`, `frontend/src/components/match-first/NeighborhoodDetail.tsx`, `frontend/src/test/match-first-neighborhood-detail.test.tsx` | House/building selection is only exposed for reliable/candidate/manual fallback building records. With current missing 3D/no reliable address seed data, the list fallback remains usable and does not navigate to Dossier or implement Phase 7. | Frontend detail tests verify no-reliable-address list fallback and no `/run` or Dossier route behavior. |
| PRD Sections 20.3, 21.2; Constitution XV | `frontend/src/services/matchFirstAnalytics.ts`, `frontend/src/components/match-first/NeighborhoodDetail.tsx`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json` | Privacy-safe events cover detail open, building-layer failure, amenity-layer failure, and missing-3D fallback. Localized fallback keys cover missing 3D, layer/building/amenity failures, no reliable address, and selected-neighborhood loading. | `cd frontend && npm run test -- src/test/match-first-neighborhood-detail.test.tsx src/test/match-first-copy-guard.test.ts src/test/match-i18n.test.ts` passed. |
| PRD A11Y-1 to A11Y-6; performance/canvas fallback requirement | `frontend/src/components/match-first/NeighborhoodDetail.tsx`, `frontend/src/components/match-first/NeighborhoodBuildingLayer.tsx`, `frontend/src/test/match-first-neighborhood-detail.test.tsx`, `frontend/src/test/match-first-a11y.test.tsx` | Recommendations remain usable from the non-map list path, the selected-neighborhood fallback is accessible, and the canvas fallback reports a drawn nonblank state in component tests. | `cd frontend && npm run test -- src/test/match-first-neighborhood-detail.test.tsx src/test/match-first-a11y.test.tsx` passed as part of the 51-test focused command. |

## Phase 6 Review Repair 2026-05-17

| Review finding / risk | Exact work completed | Files changed | Verification |
| --- | --- | --- | --- |
| Amenity endpoint failure could clear selected-neighborhood map/layer data and block the scoped building/2D fallback path. | Replaced the coupled detail fetch `Promise.all` with settled request handling. Summary and map-layer results now survive an amenity failure, amenities show their localized unavailable state, and the selected-bounds building fallback still runs. | `frontend/src/components/match-first/NeighborhoodDetail.tsx`, `frontend/src/test/match-first-neighborhood-detail.test.tsx` | Red-first: `cd frontend && npm run test -- src/test/match-first-neighborhood-detail.test.tsx -- -t "keeps selected map"` first failed because the selected map lost display bounds after an amenity failure. Final targeted command passed with the strict bounds regression: `cd frontend && npm run test -- src/test/match-first-neighborhood-detail.test.tsx -- -t "keeps selected map|uses the exact selected"` passed with 2 tests. |
| Frontend no-national-3D coverage checked only that one national bbox string was absent. | Parsed the building request URL and asserted `bounds_rd` exactly equals the selected map-layer `allowed_bounds_rd`, including a non-round decimal bounds regression. | `frontend/src/test/match-first-neighborhood-detail.test.tsx` | `cd frontend && npm run test -- src/test/match-first-neighborhood-detail.test.tsx -- -t "uses the exact selected"` passed as part of the final targeted command. |
| Backend national/out-of-scope coverage lacked a near-edge regression. | Added a centimeter-scale RD New bounds escape in addition to the national-bounds rejection. | `backend/tests/test_match_neighborhood_layers.py` | `cd backend && pytest -q tests/test_match_neighborhood_layers.py -k "building_requests"` passed; full `cd backend && pytest -q tests/test_match_neighborhood_layers.py` passed with 4 tests. |
| CI confidence before commit/push needed full local gate evidence. | Ran the local equivalents of the active GitHub workflow jobs plus touched-file lint. | Frontend/backend test and build surfaces; documentation updated here and in handoff. | `cd frontend && npm run test` passed; `cd frontend && npm run build` passed; `npm run landing:test:e2e` passed with 23 tests and 1 skipped; `cd backend && ruff check .` passed; `cd backend && pytest -x -q -m "not live and not visual and not benchmark"` passed with 1337 tests, 8 skipped, and 17 deselected; `cd backend && pytest -x -q -m "benchmark"` passed with 2 tests and 1360 deselected; `cd backend && pytest -x -q -m "visual"` collected 4 skipped and 1358 deselected locally. |

Final verified commands for Phase 6:

- `.specify/scripts/powershell/check-prerequisites.ps1 -Json -RequireTasks -IncludeTasks` returned `FEATURE_DIR` as `C:\Users\milos\buurt-check\specs\002-match-first-revamp` with required docs available.
- Checklist status: `requirements.md` passed with 76/76 completed items.
- `cd backend && pytest -q tests/test_match_neighborhood_layers.py` passed with 4 tests.
- `cd backend && ruff check app tests/test_match_neighborhood_layers.py` passed.
- `cd frontend && npm run test -- src/test/match-first-neighborhood-detail.test.tsx src/test/match-first-results-map.test.tsx src/services/matchFirstApi.test.ts src/test/match-first-a11y.test.tsx src/test/match-first-copy-guard.test.ts src/test/match-i18n.test.ts` passed with 51 tests.
- `cd frontend && npm run test -- src/services/matchFirstAnalytics.test.ts` passed with 7 tests.
- `cd frontend && npx eslint src/components/match-first/ResultsMap.tsx src/components/match-first/RecommendationCard.tsx src/components/match-first/RecommendationList.tsx src/components/match-first/NeighborhoodDetail.tsx src/components/match-first/NeighborhoodBuildingLayer.tsx src/components/match-first/AmenityTags.tsx src/components/match-first/HouseSelectionPanel.tsx src/test/match-first-neighborhood-detail.test.tsx src/test/match-first-results-map.test.tsx src/services/matchFirstApi.ts src/services/matchFirstApi.test.ts src/services/matchFirstAnalytics.ts` passed.
- `cd frontend && npm run build` passed. Build emitted a lazy `NeighborhoodDetail-D6byeTfP.js` chunk at 11.40 kB / 3.24 kB gzip and `NeighborhoodDetail-BkE5_SO2.css` at 3.37 kB / 1.07 kB gzip.
- 2026-05-17 review-repair verification additionally passed:
  `cd frontend && npm run test -- src/test/match-first-neighborhood-detail.test.tsx src/test/match-first-results-map.test.tsx src/services/matchFirstApi.test.ts src/test/match-first-a11y.test.tsx src/test/match-first-copy-guard.test.ts src/test/match-i18n.test.ts` with 53 tests;
  `cd frontend && npm run test`; `cd frontend && npm run build`;
  `npm run landing:test:e2e`; `cd backend && ruff check .`;
  `cd backend && pytest -x -q -m "not live and not visual and not benchmark"`;
  `cd backend && pytest -x -q -m "benchmark"`; and
  `cd backend && pytest -x -q -m "visual"` with local visual tests skipped.

Residual Phase 6 risks:

- Real selected-neighborhood 3D rendering remains a provider/data integration risk; the implemented path correctly scopes building requests and shows the localized 2D fallback for current missing 3D data.
- Browser-level Playwright/mobile-performance proof for the selected-neighborhood canvas remains open before production release.
- Direct `npx eslint src/App.tsx` still reports the repo's pre-existing `react-refresh/only-export-components` issue for exported helper `getMatchRouteMotionProps` plus an unrelated hook dependency warning; targeted lint for the new/changed Phase 6 files passed.
- Phase 7 house-to-existing-Dossier bridge and browser/mobile round-trip proof
  have since been implemented in the section below.

## Phase 7 Dossier Bridge

Status: **PASS for Phase 7**. Selected server-side house/building candidates
resolve to existing Dossier routes with match context, and ambiguous server
candidates call the backend PDOK Locatieserver reverse path to return selectable
nearby address choices before Dossier entry. Deterministic first/second seed
candidates, provider-backed ambiguous seed candidates, and selected-candidate
Dossier entry are covered by backend and browser tests. Dossier keeps the
persistent localized Back to match map action; direct address-search Dossier
entry remains unchanged. The latest repair tightened the backend trust
boundary, stale/no-address/manual recovery, analytics timing/privacy, touch
targets, reduced-motion handling, and browser round-trip evidence without
starting Phase 8.

| PRD / constitution item | Current status | Verification |
| --- | --- | --- |
| PRD FR-D1, FR-D5; Section 21.5; Constitution VI | Passed. `POST /api/match/dossier/from-building` validates completed match context, requires selected result ID/rank, rejects stale result metadata, returns no-store responses, avoids checkout `session_id` query reuse, validates route-context metadata, rejects spoofed client building/address/lookup/selected-house/candidate IDs, returns stable `match.dossier.invalid_vbo_id` for malformed VBO input, and emits `resolved`, `candidates`, `manual_required`, or `unavailable` responses. Ambiguous server-side house candidates use backend PDOK Locatieserver reverse lookup for nearby address choices. Candidate selection is validated against the server-side selected-neighborhood candidate set and route context uses server values. Bridge logic lives in the existing scoped building service rather than a new Dossier module. | `cd backend && ruff check app tests/test_match_neighborhood_layers.py` passed. `cd backend && pytest -q tests/test_match_neighborhood_layers.py tests/test_export_entitlement.py tests/test_reports_api.py` passed with 42 tests. `cd backend && pytest -q tests/test_locatieserver.py -k reverse` passed. Bridge tests cover resolved, PDOK-backed candidates, selected candidate to Dossier, manual_required, unavailable, provider-empty/provider-failure manual recovery, stale result-set/metadata rejection, required selected result identity, spoofed candidate field rejection, spoofed return selected-house rejection, spoofed candidate IDs, stable invalid-VBO errors, and no-store responses. |
| PRD FR-D1, FR-D2, FR-D5; Constitution IX | Passed. House click calls the bridge, stores selected session/job/result/preference vector/neighborhood/result rank/house/map center/zoom/list scroll/mobile mode/language state, renders returned provider-labelled candidate address choices when required, posts only the selected server candidate ID for candidate selection, opens returned `#/address/{vbo_id}` routes only when App receives structured `match_return` context with `sessionId` and `target`, records Dossier-open only after the returned lookup/VBO hydrates, and returns to the selected match state without `/run` or survey restart. Candidate choices have unique accessible names/descriptions, keyboard activation, 44 px targets, focus-visible styling, EN/NL keys, manual search, and Back to results. | `cd frontend && npm run test -- src/test/match-first-neighborhood-detail.test.tsx src/App.test.tsx src/services/matchFirstApi.test.ts src/services/matchFirstAnalytics.test.ts src/test/match-first-routing.test.tsx src/services/matchSessionStorage.test.ts src/test/match-i18n.test.ts` passed after candidate UI/API/analytics/i18n coverage. `cd frontend && npm run test:e2e -- tests/e2e/match-first-dossier-roundtrip.spec.ts` passed with 10 tests and 2 intentional skips. Cross-browser tests cover mobile/reduced-motion round trip; the Chromium backend-integrated provider proof creates a real completed match, opens ambiguous seed house 3, receives PDOK Locatieserver reverse-backed candidates from the real backend bridge, opens Dossier, returns to match map, and verifies no `/run` during Dossier open or return. |
| PRD FR-D3; Dossier/risk-card contract; Constitution VI | Passed for scoped changes. Existing Dossier modules were not rewritten. The app uses existing Dossier route loading plus the persistent Back to match map action when match-return context exists. Direct Dossier entry from address search remains supported because match-return parameters are optional. | `cd frontend && npm run build` passed. `cd frontend && npm run test -- src/components/DossierSheet.test.tsx src/components/RiskTilesGrid.test.tsx src/components/ExportBottomSheet.test.tsx src/components/ActionBar.test.tsx src/components/BuildingFactsCard.test.tsx src/components/ViewingChecklist.test.tsx --runInBand` passed with 89 tests. `cd backend && pytest -q tests/test_export_entitlement.py tests/test_reports_api.py` passed with 22 tests. |
| PRD Sections 20.4 and 21.5; Constitution XV | Passed for stable local analytics. `match_dossier_opened`, `match_no_reliable_address_shown`, `match_back_to_map_clicked`, `match_back_to_map_return_success`, and `match_back_to_map_return_failed` are allowlisted with privacy-safe IDs/codes only. Exact address/VBO/candidate/lookup IDs are not allowlisted or stored; Dossier-open records only after App hydrates the returned Dossier lookup/VBO; missing/malformed `match_return` and lookup failures do not record Dossier-open; return success/failure records after target match hydration. | `cd frontend && npm run test -- src/services/matchFirstAnalytics.test.ts` passed as part of the required focused frontend command. `src/App.test.tsx` covers Dossier-open timing, lookup-failure suppression, and return success/failure timing; `frontend/tests/e2e/match-first-dossier-roundtrip.spec.ts` asserts localStorage analytics for Dossier-open, missing-`match_return` rejection, lookup failure, and Back-to-map return success. |

### Phase 7 Review Gap Repair 2026-05-17

| Review finding / risk | Exact work completed | Files changed | Verification |
| --- | --- | --- | --- |
| Dossier bridge route params echoed stale `job_id`, `preference_vector_version`, selected result ID, and selected result rank after only validating `session_id`, `result_set_id`, and neighborhood visibility. | Added server-side validation against the persisted completed result. Mismatched job/vector/selected-result metadata now returns stable `409` with `match.results.stale`. | `backend/app/api/match.py`, `backend/tests/test_match_neighborhood_layers.py` | Red-first: `cd backend && pytest -q tests/test_match_neighborhood_layers.py -k "dossier_bridge_rejects_stale_return_metadata"` first failed because stale metadata returned 200. Final `cd backend && pytest -q tests/test_match_neighborhood_layers.py` passed with 10 tests; `cd backend && ruff check app tests/test_match_neighborhood_layers.py` passed. |
| Returned selected-neighborhood detail could overwrite the exact map center/zoom restored from Dossier Back to match map. | `NeighborhoodDetail` now accepts restored map/list state only when session, job, result set, vector version, selected neighborhood, selected recommendation, and rank match the active completed result; valid returned center, zoom, list scroll, mobile mode, selected house, and language are preserved exactly. | `frontend/src/components/match-first/NeighborhoodDetail.tsx`, `frontend/src/test/match-first-neighborhood-detail.test.tsx` | Red-first: `cd frontend && npm run test -- src/test/match-first-neighborhood-detail.test.tsx --runInBand` first failed because `[52.36, 4.9]` and zoom `13` were overwritten by centroid/zoom `14`. Final detail/App tests passed: `cd frontend && npm run test -- src/test/match-first-neighborhood-detail.test.tsx src/App.test.tsx`. |
| Multiple house actions exposed the same accessible name, so screen-reader users could not distinguish candidates. | Added `matchFirst.neighborhood.openDossierForHouse` in EN/NL and used it as the per-house accessible name while preserving visible `Open Dossier` / `Open dossier` labels. The address-candidate description is now associated through `aria-describedby`. | `frontend/src/components/match-first/HouseSelectionPanel.tsx`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json`, `frontend/src/test/match-first-neighborhood-detail.test.tsx` | Red-first detail test failed when `Open Dossier for house 1` was absent. Final `cd frontend && npm run test -- src/test/match-first-neighborhood-detail.test.tsx` and `cd frontend && npm run test -- src/test/match-i18n.test.ts` passed through focused suites. |

### Phase 7 Stop-Phase-8 Repair 2026-05-17

| Review finding / risk | Exact work completed | Files changed | Verification |
| --- | --- | --- | --- |
| Backend bridge still trusted client-supplied building/address identifiers and resolved only one candidate once result context passed. | Added tests and implementation requiring `selected_result_id` and `selected_result_rank`; the bridge now resolves the selected-neighborhood candidate set server-side by `building_id`, supports deterministic first and second candidates from the same scoped-building source, rejects mismatched `building_id`, `vbo_id`, `address_id`, `lookup_id`, and return-context `selected_house_id` with `match.dossier.building_not_found`, and returns stable `match.dossier.invalid_vbo_id` for malformed VBOs. Route context uses server candidate values. | `backend/app/api/match.py`, `backend/app/models/match.py`, `backend/app/services/match/buildings.py`, `backend/tests/test_match_neighborhood_layers.py` | Red-first: `cd backend && pytest -q tests/test_match_neighborhood_layers.py -k "selected_result_identity or client_spoofed"` failed before the fix. Additional red-first repairs: `cd backend && pytest -q tests/test_match_neighborhood_layers.py -k "spoofed_return_selected_house_id"` failed before selected-house context hardening, and `cd backend && pytest -q tests/test_match_neighborhood_layers.py -k "second_selected_building or invalid_vbo"` failed before multi-candidate lookup and stable invalid-VBO handling. Final required backend commands passed, including bridge, export entitlement, and reports API tests. |
| Backend bridge lacked candidate-address statuses and selected-candidate validation. | Added `resolved`, `candidates`, `manual_required`, and `unavailable` response statuses, server-generated `candidate_addresses` with stable candidate IDs/display keys/params/reliability/source refs/fallback codes, selected-candidate-to-Dossier resolution, manual-required recovery, no-store assertions, and spoofed selected-candidate rejection. Ambiguous server-side candidates now call the backend PDOK Locatieserver reverse path; provider-empty/provider-failure cases recover to `manual_required`. Candidate routes are built from server-side candidate values, not client-supplied address/VBO/lookup IDs. | `backend/app/models/match.py`, `backend/app/services/match/buildings.py`, `backend/app/services/locatieserver.py`, `backend/tests/test_match_neighborhood_layers.py`, `backend/tests/test_locatieserver.py` | Red-first: `cd backend && pytest -q tests/test_locatieserver.py -k reverse` failed because `reverse_addresses` was missing; `cd backend && pytest -q tests/test_match_neighborhood_layers.py -k "provider or candidate_addresses or selected_candidate_address"` failed because ambiguous houses were deterministic/non-provider and selected candidates were not provider-backed. Final provider-focused backend tests passed; final required backend commands passed. |
| Bridge stale errors were shown as no-reliable-address failures, invalid resolved routes had no recovery, and candidate choices were absent. | `match.results.stale` and result-not-found API failures now show the stale/unavailable results recovery. No reliable address, manual-required, invalid resolved bridge routes, and returned provider-labelled candidate-address choices are distinct recoverable states with localized Search manually and Back to results actions. Candidate choices are keyboard-usable buttons with unique accessible names/descriptions, 44 px touch targets, focus-visible styling, and EN/NL translation keys only. | `frontend/src/components/match-first/HouseSelectionPanel.tsx`, `frontend/src/components/match-first/NeighborhoodDetail.tsx`, `frontend/src/components/match-first/NeighborhoodDetail.css`, `frontend/src/services/matchFirstApi.ts`, `frontend/src/types/matchFirst.ts`, `frontend/src/i18n/en.json`, `frontend/src/i18n/nl.json`, `frontend/src/test/match-first-neighborhood-detail.test.tsx` | Red-first: candidate UI tests initially failed because candidate choices were not rendered, manual-required rendered no-reliable copy, and candidate touch-target CSS was absent. Final required frontend focused test command passed. |
| Analytics could store exact address/VBO IDs and Dossier-open fired on route acceptance instead of Dossier hydration. | Removed `address_id` from the match-first analytics allowlist; moved `match_dossier_opened` recording out of `NeighborhoodDetail` and into App after the returned lookup/VBO hydrates; hardened `App.openMatchDossierRoute` so bridge routes require structured `match_return` with `sessionId` and `target`; invalid bridge routes and lookup failures do not record Dossier-open; `match_back_to_map_return_success` and failure now fire from ResultsMap/NeighborhoodDetail hydration callbacks. | `frontend/src/App.tsx`, `frontend/src/components/match-first/NeighborhoodDetail.tsx`, `frontend/src/components/match-first/ResultsMap.tsx`, `frontend/src/services/matchFirstAnalytics.ts`, `frontend/src/services/matchFirstAnalytics.test.ts`, `frontend/tests/e2e/match-first-dossier-roundtrip.spec.ts` | Red-first: analytics test initially failed because `0363010000123456` was stored. App-level timing tests cover no Dossier-open before hydration and no event on lookup failure. E2E inspects localStorage analytics for Dossier-open, missing-`match_return` rejection, lookup failure, and back-to-map return success. Final required frontend focused test command passed. |
| T-074 browser/mobile round-trip proof, touch-target proof, reduced-motion proof, and provider-backed proof were incomplete. | Added/updated `frontend/tests/e2e/match-first-dossier-roundtrip.spec.ts` covering mobile viewport and explicit reduced-motion emulation: house click shows candidate choices, selected candidate opens existing `#/address` Dossier, persistent Back to match map restores exact selected neighborhood/map/list/house state, a second house opens without `/run`, missing `match_return` is rejected, and lookup failure suppresses Dossier-open analytics. Added a Chromium-only backend-integrated provider proof that creates a real completed match, lets the real backend bridge call PDOK Locatieserver reverse for ambiguous seed house 3, opens Dossier from the selected provider candidate, returns to match map, and asserts no `/run` during Dossier open or return. House-selection, candidate, and recovery controls have 44 px touch targets and focus-visible styling. | `frontend/tests/e2e/match-first-dossier-roundtrip.spec.ts`, `frontend/src/App.tsx`, `frontend/src/components/match-first/NeighborhoodDetail.css`, `frontend/src/test/match-first-neighborhood-detail.test.tsx` | `cd frontend && npm run test:e2e -- tests/e2e/match-first-dossier-roundtrip.spec.ts` passed with 10 tests and 2 intentional skips. Firefox/WebKit skip only the backend-integrated provider proof to avoid shared local DB races; their UI round-trip proof still runs. |

Additional repair verification:

- `cd frontend && npm run build` passed.
- 2026-05-17 commit-readiness verification also passed the active CI-equivalent
  gates: `cd backend && ruff check .`; `cd backend && pytest -x -q -m "not live and not visual and not benchmark"` with 1354 passed, 8 skipped, and 17 deselected; `cd frontend && npm run build`; `cd frontend && npm run test`; and `npm run landing:test:e2e` with 23 passed and 1 skipped.
- `cd backend && pytest -x -q -m "not live"` also passed locally with 1356
  passed, 12 skipped, and 11 deselected.
- `cd backend && pytest -x -q -m "visual"` collected 4 skipped and 1375
  deselected locally; `cd backend && pytest -x -q -m "benchmark"` passed with
  2 passed and 1377 deselected.
- `cd frontend && npm run test -- --run src/test/match-first-copy-guard.test.ts`
  passed after the final type-only copy-guard repair in
  `HouseSelectionPanel.tsx`.
- Existing Dossier preservation gates remained covered by the required backend entitlement/report command. Previous frontend Dossier component gates still stand from the earlier Phase 7 bridge pass.
- `git diff --check` passed with CRLF normalization warnings only.

Final verified commands for Phase 7:

- `cd backend && ruff check .`
- `cd backend && ruff check app tests/test_match_neighborhood_layers.py`
- `cd backend && pytest -x -q -m "not live and not visual and not benchmark"` passed with 1354 passed, 8 skipped, and 17 deselected.
- `cd backend && pytest -x -q -m "not live"` passed with 1356 passed, 12 skipped, and 11 deselected.
- `cd backend && pytest -x -q -m "visual"` collected 4 skipped and 1375 deselected locally.
- `cd backend && pytest -x -q -m "benchmark"` passed with 2 passed and 1377 deselected.
- `cd backend && pytest -q tests/test_match_neighborhood_layers.py tests/test_export_entitlement.py tests/test_reports_api.py` passed with 42 tests.
- `cd backend && pytest -q tests/test_locatieserver.py -k reverse`
- `cd frontend && npm run test -- src/test/match-first-neighborhood-detail.test.tsx src/App.test.tsx src/services/matchFirstApi.test.ts src/services/matchFirstAnalytics.test.ts src/test/match-first-routing.test.tsx src/services/matchSessionStorage.test.ts src/test/match-i18n.test.ts`
- `cd frontend && npm run test`
- `cd frontend && npm run test -- --run src/test/match-first-copy-guard.test.ts`
- `cd frontend && npm run test:e2e -- tests/e2e/match-first-dossier-roundtrip.spec.ts` passed with 10 tests and 2 intentional skips.
- `cd frontend && npm run build`
- `npm run landing:test:e2e` passed with 23 passed and 1 skipped.
- `git diff --check` passed with CRLF normalization warnings only.

Blocked / residual Phase 7 risks:

- `cd frontend && npm run lint` failed on pre-existing repo-wide lint issues in
  unrelated files, including React Compiler set-state/ref rules, Fast Refresh
  export rules, old test unused variables, and unrelated `any` usages. The
  active GitHub CI workflow does not run this frontend lint command; the Phase 7
  build, full frontend test suite, landing smoke, and backend CI-equivalent
  gates passed.
- No Phase 7 provider-backed candidate blocker remains. The backend-integrated
  provider proof is Chromium-only to avoid local shared-DB races; Firefox and
  WebKit still run the UI-mocked Dossier round-trip proof.

## Punch-List Fixes 2026-05-13 to 2026-05-14

- Match-return Dossier URLs without `lookup`, such as `#/address/{vbo_id}?match_return=...`, now keep the user in the Dossier route and render a localized address-unavailable shell instead of redirecting to Search.
- The Back to match map action is rendered by the Dossier route wrapper whenever match-return context exists, so it remains available through loading, empty, error, and successful address states.
- Match run `completed` / `running` state is no longer trusted from `localStorage`. Until backend run/status/results endpoints are wired, direct success and results routes render neutral localized shells rather than implying that matching has completed.
- Survey answers are scoped by match session ID, preventing a new session from inheriting stale answers from an earlier one.
- `App.tsx` reuses the shared route parser for structured `match_context`, preserving selected neighborhood, selected house, map center, zoom, and list scroll on cold-start Dossier URLs.
- Direct match neighborhood URLs do not trust persisted Dossier return context from `localStorage`; that context is only reused for the active Dossier back action.
- Failure and unavailable copy now has explicit bilingual keys, enforced by `frontend/src/test/match-i18n.test.ts`, for no strong matches, slow backend, failed backend, completed-with-fallback, no reliable address, no-result states, missing 3D, neutral results placeholders, and Dossier address-unavailable recovery.
- Neutral placeholder copy is used for direct unverified success, results, and neighborhood routes until backend-backed completion is verified; explicit progress and failed-backend copy is reserved for confirmed run/status states.
- Dossier match-return context now preserves mobile map/list mode plus selected result ID and rank, reducing context loss when returning from a house Dossier to the match map.
- Settings is hidden during match-first onboarding screens so the flow remains one decision per screen.
- Match route transitions respect reduced-motion preferences.
- Failed match progress states now expose a localized retry action that retries the same match session instead of forcing users to restart the survey.
- Direct Phase 1 survey question routes above step 1 are rejected until the final survey question set lands in Phase 2.
- Back-to-match-map context is rehydrated after refresh only when the user has actually activated the Dossier Back to match map action; passive or arbitrary localStorage context still cannot unlock direct neighborhood placeholders.
- Survey review copy now says Housing goal / Woondoel instead of Search type / Zoektype.
- Direct neighborhood placeholder routes are included in the Phase 1 accessibility coverage.
- Invalid Phase 1 match routes now recover back to the active match survey as the focused primary action, with address search kept as a secondary option.
- Restored selected-neighborhood placeholders now use neutral restored-context copy and expose a Back to survey action while preserving route context attributes for later map/detail hydration.
- App-driven route changes no longer suppress the next real hash route, and browser Back/Forward route changes now re-run the Phase 1 hash-route parser.
- Loaded Dossier exits that send users back to search now preserve match-return context so the existing Back to match map path survives the next address selection.

## Current Phase 1 Limits

- The survey is intentionally one dummy question for Phase 1 shell completion. Final 10-12 question content, backend answer persistence, and preference-vector generation remain Phase 2 work.
- The final survey action now starts backend matching, polls run status, verifies result payload hydration before success, and keeps the visible results map as a Phase 5 placeholder.
- Neighborhood-only 3D building loading is not changed in this phase; no national 3D loading path was introduced.
