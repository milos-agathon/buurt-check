# Frontend observability
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=dev

# Pricing fallback before /api/pricing resolves
VITE_DOSSIER_PRICE_EUR=14.99

# Product behavior: first full dossier is free once per device/browser profile
# (tracked in localStorage as `buurt-check:first-dossier-used`).

# Preview mode toggles (temporary merchandising review)
# true/false values. Defaults are true when omitted.
VITE_PREVIEW_DISABLE_PAYMENTS=true
VITE_PREVIEW_FORCE_FULL_DOSSIER_VIEW=true

# Sunlight analysis tuning
# Keep worker enabled unless debugging.
VITE_SUNLIGHT_USE_WORKER=true
# Explicit scene-level culling radius for sunlight occlusion checks.
VITE_SUNLIGHT_CULL_DISTANCE_METERS=120
# Number of roof points sampled for SVF averaging.
VITE_SVF_SAMPLE_POINTS=5
